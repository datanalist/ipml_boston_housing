# Отчеты об экспериментах

Этот раздел содержит автоматически сгенерированные отчеты о ML экспериментах.

---

## 📊 Обзор

Отчеты создаются автоматически после завершения экспериментов и включают:

- 📈 Визуализации метрик
- 📋 Сравнительные таблицы
- 🏆 Лучшие модели
- 💡 Рекомендации

---

## 🔧 Генерация отчетов

### Автоматическая генерация

Отчеты генерируются автоматически с помощью скрипта:

```bash
python scripts/generate_experiment_report.py
```

### Ручная генерация

```python
from scripts.generate_experiment_report import ExperimentReportGenerator

generator = ExperimentReportGenerator()
report_path = generator.generate_report(
    title="Мой отчет об экспериментах",
    output_file="my_report.md"
)
```

---

## 📚 Доступные отчеты

| Отчет | Дата | Описание |
|-------|------|----------|
| [Последние эксперименты](generated/latest_experiments.md) | Актуально | Автоматически обновляемый отчет |

!!! note "Сравнительный анализ"
    Для сравнительного анализа моделей используйте MLflow UI (http://localhost:5000) или генерируйте отчёт командой `python scripts/generate_experiment_report.py`.

---

## 📈 Структура отчета

Каждый отчет включает:

### 1. Общая статистика
- Количество экспериментов
- Лучшая/худшая модель
- Средние метрики

### 2. Детали лучшей модели
- Метрики train/test
- Время обучения
- Параметры

### 3. Визуализации
- График сравнения метрик
- Scatter plot RMSE vs R²
- Дополнительные графики

### 4. Сравнительная таблица
- Все модели с метриками
- Выделение лучших значений

### 5. Выводы и рекомендации
- Анализ результатов
- Проверка на переобучение
- Практические рекомендации

---

## 🔄 Интеграция с Airflow

Отчеты могут генерироваться автоматически после завершения DAG:

```python
from airflow.operators.python import PythonOperator

generate_report = PythonOperator(
    task_id='generate_report',
    python_callable=lambda: ExperimentReportGenerator().generate_report(),
    dag=dag,
)

train_models >> generate_report
```

---

## 📊 Примеры визуализаций

### Сравнение метрик

После генерации отчёта в папке `docs/reports/plots/` появятся графики:

- `metrics_comparison.png` — горизонтальные bar charts для всех основных метрик
- `rmse_vs_r2.png` — scatter plot с цветовой кодировкой по типу модели

!!! info "Генерация графиков"
    Для создания визуализаций запустите:
    ```bash
    python scripts/generate_experiment_report.py
    ```

---

## 🎨 Кастомизация

### Изменение стиля графиков

```python
import seaborn as sns
import matplotlib.pyplot as plt

# Настройка стиля
sns.set_style("darkgrid")
plt.rcParams["figure.figsize"] = (14, 8)
plt.rcParams["font.size"] = 12

generator = ExperimentReportGenerator()
generator.generate_report()
```

### Добавление дополнительных метрик

Отредактируйте `scripts/generate_experiment_report.py`:

```python
def create_custom_plot(self, df: pd.DataFrame) -> Path:
    """Создание кастомного графика."""
    plt.figure(figsize=(12, 6))
    # Ваш код визуализации
    plt.savefig(output_path)
    return output_path
```

---

## 📁 Структура файлов

```
docs/reports/
├── index.md                      # Этот файл
├── generated/                    # Автогенерируемые отчеты
│   ├── latest_experiments.md
│   └── experiment_report_*.md
├── plots/                        # Графики
│   ├── metrics_comparison.png
│   ├── rmse_vs_r2.png
│   └── ...
└── comparison.md                 # Ручной сравнительный анализ
```

---

## 🔍 Просмотр отчетов

### В документации

После сборки документации (`mkdocs serve`):

```bash
open http://localhost:8000/reports/generated/latest_experiments/
```

### Напрямую

```bash
cat docs/reports/generated/latest_experiments.md
```

### В GitHub

Отчеты автоматически публикуются на GitHub Pages.

---

## ⚙️ Настройка автоматизации

### Добавить в Makefile

```makefile
.PHONY: generate-reports
generate-reports:
    @echo "Generating experiment reports..."
    uv run python scripts/generate_experiment_report.py
    @echo "Reports generated successfully!"
```

Использование:

```bash
make generate-reports
```

### Интеграция с CI/CD

Добавьте в `.github/workflows/experiments.yml`:

```yaml
- name: Generate reports
  run: |
    python scripts/generate_experiment_report.py
    git add docs/reports/generated/
    git commit -m "chore: update experiment reports"
    git push
```

---

## 💡 Советы

!!! tip "Регулярная генерация"
    Генерируйте отчеты после каждого значимого эксперимента для отслеживания прогресса.

!!! info "Версионирование"
    Отчеты сохраняются с датой в имени файла для версионирования.

!!! warning "Размер файлов"
    Графики в высоком разрешении могут занимать много места. Настройте `dpi` в коде.

---

## 🆘 Troubleshooting

### Проблема: Нет данных для отчета

**Решение:**
```bash
# Убедитесь, что есть файл с результатами экспериментов
ls data/experiments/results_summary.csv

# Или запустите эксперименты
python src/modeling/train_hydra.py --multirun model=ridge,lasso,rf
```

### Проблема: Ошибка при генерации графиков

**Решение:**
```bash
# Установите необходимые зависимости
uv add matplotlib seaborn jinja2

# Проверьте, что каталоги существуют
mkdir -p docs/reports/generated docs/reports/plots
```

---

## 📚 Дополнительные ресурсы

- [API Reference](../api/index.md) — документация кода
- [Примеры использования](../examples/index.md) — практические примеры
- [MLflow трекинг](../guides/MLFLOW+DVC+MINIO.md) — интеграция с MLflow
