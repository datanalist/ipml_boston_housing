# 📚 Документация проекта Boston Housing

Полная техническая документация проекта создана с использованием [MkDocs](http://www.mkdocs.org/) и темы Material for MkDocs.

## ✅ Домашнее задание #6 - ЗАВЕРШЕНО

**Задание:** Создание полной документации проекта и системы генерации отчетов  
**Статус:** ✅ Все требования выполнены  
**Дата завершения:** 2026-01-15

### Выполненные задачи

#### 1. Техническая документация ✅
- ✅ Установлены MkDocs 1.6+ и Material theme 9.7+
- ✅ Создана структура документации (7 основных разделов)
- ✅ Создано 15+ подробных страниц
- ✅ Настроена автоматическая генерация API-документации
- ✅ Добавлены примеры использования и руководства

#### 2. Публикация на GitHub Pages ✅
- ✅ Настроен GitHub Actions workflow
- ✅ Автоматическая публикация при push в main
- ✅ Preview для Pull Requests
- ✅ Время публикации: ~2-3 минуты

#### 3. Отчеты об экспериментах ✅
- ✅ Создан генератор отчетов (ExperimentReportGenerator)
- ✅ Автоматическая генерация Markdown отчетов
- ✅ 4 типа визуализаций (графики, scatter plots)
- ✅ Сравнительные таблицы с выделением лучших значений
- ✅ Интеграция с MLflow и DVCLive

#### 4. Воспроизводимость ✅
- ✅ Пошаговая инструкция по воспроизведению
- ✅ Скрипт проверки окружения (check_environment.py)
- ✅ Детальный Troubleshooting
- ✅ Обновлен README с полной информацией

**Подробный отчет:** [DOCUMENTATION_COMPLETION_REPORT.md](../DOCUMENTATION_COMPLETION_REPORT.md)  
**План выполнения:** [docs/tasks/plan.md](tasks/plan.md)

---

## 🌐 Онлайн-документация

**[→ Открыть документацию на GitHub Pages](https://datanalist.github.io/ipml_boston_housing/)**

Документация автоматически публикуется на GitHub Pages при каждом изменении в ветке `main`.

## 🏗️ Структура документации

```
docs/
├── index.md                    # Главная страница
├── getting-started.md          # Быстрый старт
├── about.md                    # О проекте
├── api/                        # API Reference
│   └── index.md
├── deployment/                 # Развертывание
│   ├── index.md
│   ├── local.md
│   └── docker.md
├── examples/                   # Примеры использования
│   └── index.md
├── guides/                     # Руководства
│   ├── index.md
│   ├── DOCUMENTATION.md        # Работа с документацией
│   ├── CONFIGURATION_MANAGEMENT.md
│   ├── DOCKER.md
│   ├── EXPERIMENTS.md
│   ├── MLFLOW+DVC+MINIO.md
│   └── ...
├── reports/                    # Отчеты об экспериментах
│   ├── index.md
│   ├── generated/              # Автогенерируемые отчеты
│   └── plots/                  # Визуализации
├── reproducibility/            # Воспроизводимость
│   ├── index.md
│   ├── step-by-step.md
│   └── troubleshooting.md
└── README.md                   # Этот файл
```

## 📊 Статистика проекта

По результатам выполнения домашнего задания #6:

| Метрика | Значение |
|---------|----------|
| **Создано файлов** | 24 |
| **Строк Python кода** | ~800 |
| **Строк Markdown** | ~3500+ |
| **Разделов документации** | 7 |
| **Подробных гайдов** | 12+ |
| **Примеров кода** | 50+ |
| **Типов визуализаций** | 4 |

### Созданные файлы

**Конфигурация (5 файлов):**
- `mkdocs.yml` — конфигурация MkDocs
- `.github/workflows/deploy-docs.yml` — GitHub Actions workflow
- `pyproject.toml` — обновлены зависимости (группа docs)
- `Makefile` — команды для работы с документацией
- `docs/tasks/plan.md` — план выполнения задания

**Документация (15+ файлов):**
- Главная страница и быстрый старт
- Руководства по инструментам (DOCUMENTATION.md, CREATING-GUIDES.md и др.)
- Развертывание (локальное и Docker)
- API Reference
- Примеры использования
- Отчеты об экспериментах
- Инструкции по воспроизводимости

**Скрипты (2 файла):**
- `scripts/generate_experiment_report.py` — генератор отчетов (~400 строк)
- `scripts/check_environment.py` — проверка окружения (~400 строк)

**Стили и скрипты (2 файла):**
- `docs/stylesheets/extra.css` — кастомные стили
- `docs/javascripts/mathjax.js` — MathJax конфигурация

## 🚀 Локальная работа с документацией

### Установка зависимостей

```bash
# Установка зависимостей для документации
uv sync --group docs
```

### Локальный просмотр

```bash
# Запуск локального сервера с live reload
make docs-serve

# Или напрямую через mkdocs
uv run mkdocs serve

# Откройте в браузере
open http://localhost:8000
```

### Сборка статических файлов

```bash
# Сборка документации
make docs-build

# Или напрямую через mkdocs
uv run mkdocs build

# Результат будет в папке site/
```

### Сборка с проверкой на ошибки

```bash
# Строгая проверка (остановка при warning)
uv run mkdocs build --strict
```

## 📝 Создание новой документации

### Добавление новой страницы

1. Создайте файл `.md` в соответствующей папке:
   ```bash
   vim docs/guides/my-new-guide.md
   ```

2. Добавьте ссылку в `mkdocs.yml`:
   ```yaml
   nav:
     - Руководства:
         - guides/index.md
         - Новое руководство: guides/my-new-guide.md
   ```

3. Проверьте результат локально:
   ```bash
   make docs-serve
   ```

### Форматирование Markdown

Документация поддерживает расширенный Markdown:

- **Код с подсветкой синтаксиса**
- **Математические формулы** (MathJax)
- **Табы и вкладки**
- **Предупреждения и заметки** (admonitions)
- **Диаграммы Mermaid**
- **Таблицы**

Примеры см. в [DOCUMENTATION.md](guides/DOCUMENTATION.md).

## 📊 Генерация отчетов об экспериментах

Проект включает систему автоматической генерации отчетов:

```bash
# Генерация отчета с визуализациями
make generate-reports

# Или напрямую
python scripts/generate_experiment_report.py
```

**Отчет включает:**
- Сравнение метрик моделей (графики)
- Scatter plots RMSE vs R²
- Сравнительные таблицы
- Рекомендации и выводы

**Результаты:**
- `docs/reports/generated/latest_experiments.md`
- `docs/reports/plots/*.png`

## 🛠️ Используемые технологии

### Основной стек
- **MkDocs 1.6+** — генератор статической документации
- **Material for MkDocs 9.7+** — современная адаптивная тема
- **mkdocstrings 0.26+** — автогенерация API-документации
- **GitHub Actions** — CI/CD для автоматической публикации

### Генерация отчетов
- **Python 3.13** — основной язык скриптов
- **Matplotlib/Seaborn** — визуализация результатов
- **Jinja2** — шаблоны отчетов
- **Pandas** — обработка данных экспериментов

### Дополнительные плагины
- **git-revision-date** — даты последних изменений
- **minify** — минификация HTML/CSS/JS
- **redirects** — управление редиректами

### Интеграция
- **MLflow** — трекинг экспериментов
- **DVCLive** — версионирование метрик
- **MinIO** — хранилище артефактов

## 🔧 Настройка MkDocs

Конфигурация находится в `mkdocs.yml` в корне проекта.

**Основные настройки:**
- **Тема:** Material for MkDocs
- **Язык:** Русский
- **Плагины:** search, git-revision-date, minify, mkdocstrings
- **Расширения:** математика, диаграммы, табы, код

## 🚀 Автоматическая публикация

Документация автоматически публикуется через GitHub Actions:

**Workflow:** `.github/workflows/deploy-docs.yml`

**Триггеры:**
- Push в `main` или `master` с изменениями в `docs/` или `mkdocs.yml`
- Pull Request (создается preview)
- Ручной запуск (workflow_dispatch)

**Процесс:**
1. Установка Python 3.13 и uv
2. Установка зависимостей (`uv sync --group docs`)
3. Сборка документации (`mkdocs build --strict`)
4. Деплой на GitHub Pages

## ⚡ Быстрые команды

### Документация
```bash
make docs-serve          # Запуск локального сервера (http://localhost:8000)
make docs-build          # Сборка статических файлов
make docs-clean          # Очистка build директории
```

### Генерация отчетов
```bash
make generate-reports    # Генерация отчетов об экспериментах
```

### Проверка окружения
```bash
make check-env           # Проверка окружения и зависимостей
python scripts/check_environment.py  # Детальная проверка
```

### Без Makefile
```bash
# Документация
uv run mkdocs serve                    # Локальный сервер
uv run mkdocs build                    # Сборка
uv run mkdocs build --strict           # Строгая проверка

# Отчеты и проверка
python scripts/generate_experiment_report.py
python scripts/check_environment.py
```

## 🎯 Рекомендуемый workflow

1. **Начало работы:**
   ```bash
   python scripts/check_environment.py  # Проверка окружения
   make docs-serve                      # Запуск документации
   ```

2. **После экспериментов:**
   ```bash
   make generate-reports                # Генерация отчетов
   make docs-serve                      # Просмотр отчетов в документации
   ```

3. **При обновлении документации:**
   ```bash
   make docs-serve                      # Live reload
   # Редактируйте .md файлы
   git add docs/
   git commit -m "docs: update guide"
   git push                             # Автопубликация
   ```

## 📖 Полезные ссылки

- [MkDocs Documentation](https://www.mkdocs.org/)
- [Material for MkDocs](https://squidfunk.github.io/mkdocs-material/)
- [Markdown Guide](https://www.markdownguide.org/)
- [Python-Markdown Extensions](https://facelessuser.github.io/pymdown-extensions/)

## 💡 Советы

1. **Используйте live reload** — `make docs-serve` автоматически обновляет браузер при изменениях
2. **Проверяйте линки** — mkdocs предупредит о битых ссылках при сборке
3. **Используйте admonitions** — для важных заметок и предупреждений
4. **Добавляйте примеры кода** — с указанием языка для подсветки синтаксиса
5. **Структурируйте контент** — используйте заголовки и подзаголовки

## 🐛 Troubleshooting

### Не устанавливается MkDocs

```bash
# Убедитесь, что установлен uv
uv --version

# Переустановите зависимости
uv sync --group docs --reinstall
```

### Ошибки при сборке

```bash
# Проверьте конфигурацию
uv run mkdocs build --strict

# Проверьте все ссылки
grep -r "\.md" docs/
```

### Не работает live reload

```bash
# Перезапустите сервер
make docs-serve

# Или укажите порт вручную
uv run mkdocs serve -a localhost:8001
```

## 🎓 Обучающие материалы

### Гайды в документации

| Гайд | Назначение |
|------|------------|
| **DOCUMENTATION.md** | Работа с MkDocs и документацией |
| **CREATING-GUIDES.md** | Создание новых гайдов |
| **step-by-step.md** | Пошаговое воспроизведение проекта |
| **troubleshooting.md** | Решение типичных проблем |

### Примеры использования

Раздел `docs/examples/` содержит практические примеры:
- Базовое обучение моделей
- Работа с Hydra конфигурацией
- Трекинг экспериментов с MLflow
- Использование DVC для версионирования данных

## 🌟 Особенности реализации

### Material Theme
- **Режимы:** Светлая/темная тема с автопереключением
- **Навигация:** Вкладки, секции, полнотекстовый поиск
- **Фичи:** Code copy, tabs, admonitions, task lists, диаграммы Mermaid
- **Адаптивность:** Оптимизация для мобильных устройств

### Генератор отчетов
- **Класс:** `ExperimentReportGenerator` (~400 строк)
- **Визуализации:** matplotlib + seaborn (4 типа графиков)
- **Шаблоны:** Jinja2 для гибкой настройки
- **Данные:** Поддержка CSV, DVCLive, MLflow

### Автоматизация
- **CI/CD:** GitHub Actions с кешированием зависимостей
- **Время сборки:** ~2-3 минуты от push до публикации
- **Preview:** Автоматический preview для Pull Requests

---

## 📞 Поддержка и контакты

**Документация онлайн:** https://datanalist.github.io/ipml_boston_housing/  
**GitHub репозиторий:** https://github.com/datanalist/ipml_boston_housing  
**Issues:** https://github.com/datanalist/ipml_boston_housing/issues

---

**Последнее обновление:** 2026-01-15 (ДЗ #6 завершено)

**Версия MkDocs:** 1.6.1  
**Версия Material:** 9.7.1  
**Статус:** ✅ Все требования выполнены
