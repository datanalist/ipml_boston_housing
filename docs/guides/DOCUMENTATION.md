# 📚 Руководство по работе с документацией проекта

Это руководство описывает, как создавать, редактировать и публиковать документацию проекта.

---

## 🎯 Обзор системы документации

Проект использует **MkDocs** с темой **Material** для создания красивой и функциональной документации.

### Основные компоненты

| Компонент | Назначение |
|-----------|------------|
| **MkDocs** | Генератор статической документации из Markdown |
| **Material theme** | Современная адаптивная тема |
| **mkdocstrings** | Автогенерация API документации из docstrings |
| **GitHub Actions** | Автоматическая публикация на GitHub Pages |
| **MathJax** | Поддержка LaTeX формул |

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установка всех зависимостей для документации
uv sync --group docs

# Проверка установки
uv run mkdocs --version
```

### 2. Локальный запуск

```bash
# Запуск dev сервера с live reload
uv run mkdocs serve

# Откройте в браузере
# http://localhost:8000
```

### 3. Редактирование

1. Откройте файл `.md` в `docs/`
2. Внесите изменения
3. Сохраните файл
4. Браузер автоматически обновится

### 4. Публикация

```bash
# Сборка статических файлов
uv run mkdocs build

# Публикация на GitHub Pages (автоматически через CI/CD)
git add docs/
git commit -m "docs: update documentation"
git push
```

---

## 📁 Структура документации

```
docs/
├── index.md                    # 🏠 Главная страница
├── getting-started.md          # 🚀 Быстрый старт
├── about.md                    # ℹ️ О проекте
│
├── guides/                     # 📚 Руководства
│   ├── index.md               # Обзор руководств
│   ├── CONFIGURATION_MANAGEMENT.md
│   ├── DOCKER.md
│   ├── ENV.md
│   ├── EXPERIMENTS.md
│   ├── MLFLOW+DVC+MINIO.md
│   └── mkdocs.md              # Этот файл
│
├── deployment/                 # 🐳 Развертывание
│   ├── index.md
│   ├── local.md
│   └── docker.md
│
├── api/                        # 🔧 API Reference
│   ├── index.md
│   ├── models.md
│   ├── tracking.md
│   └── config.md
│
├── examples/                   # 💡 Примеры
│   ├── index.md
│   ├── basic-training.md
│   ├── hydra-config.md
│   └── mlflow-tracking.md
│
├── reports/                    # 📊 Отчеты
│   ├── index.md
│   ├── generated/             # Автогенерируемые отчеты
│   └── plots/                 # Графики
│
├── reproducibility/           # 🔄 Воспроизводимость
│   ├── index.md
│   ├── step-by-step.md
│   ├── dependencies.md
│   └── troubleshooting.md
│
├── stylesheets/               # 🎨 Стили
│   └── extra.css
│
└── javascripts/               # ⚙️ Скрипты
    └── mathjax.js
```

---

## ✍️ Создание новой страницы

### Шаг 1: Создайте Markdown файл

```bash
# Пример: создание нового руководства
touch docs/guides/my-guide.md
```

### Шаг 2: Добавьте контент

```markdown
# Название руководства

Краткое описание.

---

## Раздел 1

Контент раздела 1.

### Подраздел 1.1

Детали.

## Раздел 2

Контент раздела 2.

---

## Дополнительные ресурсы

- [Ссылка 1](../index.md)
- [Ссылка 2](another-guide.md)
```

### Шаг 3: Добавьте в навигацию

Отредактируйте `mkdocs.yml`:

```yaml
nav:
  - Руководства:
      - guides/index.md
      - Моё руководство: guides/my-guide.md  # Добавьте эту строку
      - Другие руководства...
```

### Шаг 4: Проверьте результат

```bash
uv run mkdocs serve
# Откройте http://localhost:8000
```

---

## 🎨 Markdown синтаксис

### Базовый синтаксис

```markdown
# H1 - Заголовок первого уровня
## H2 - Заголовок второго уровня
### H3 - Заголовок третьего уровня

**Жирный текст**
*Курсив*
`Инлайн код`

[Ссылка](https://example.com)
![Изображение](path/to/image.png)

- Маркированный список
- Элемент 2

1. Нумерованный список
2. Элемент 2
```

### Блоки кода

````markdown
```python
def hello():
    print("Hello, World!")
```

```bash
make setup
uv run python script.py
```

```python linenums="1"
# С номерами строк
def main():
    pass
```
````

### Таблицы

```markdown
| Заголовок 1 | Заголовок 2 | Заголовок 3 |
|-------------|-------------|-------------|
| Ячейка 1    | Ячейка 2    | Ячейка 3    |
| Ячейка 4    | Ячейка 5    | Ячейка 6    |
```

### Адмонишны (Callouts)

```markdown
!!! note "Заметка"
    Текст заметки.

!!! tip "Совет"
    Полезный совет.

!!! warning "Предупреждение"
    Важное предупреждение.

!!! danger "Опасность"
    Критическое предупреждение.

!!! info "Информация"
    Дополнительная информация.

!!! success "Успех"
    Что-то прошло успешно.

!!! example "Пример"
    Пример использования.
```

### Вкладки

```markdown
=== "Tab 1"

    Content of tab 1

=== "Tab 2"

    Content of tab 2

=== "Tab 3"

    Content of tab 3
```

Пример с кодом:

```markdown
=== "Python"

    ```python
    print("Hello")
    ```

=== "JavaScript"

    ```javascript
    console.log("Hello");
    ```

=== "Bash"

    ```bash
    echo "Hello"
    ```
```

### Task Lists

```markdown
- [x] Выполненная задача
- [ ] Невыполненная задача
- [ ] Еще одна задача
```

### Сноски

```markdown
Текст со сноской[^1].

Еще текст с другой сноской[^2].

[^1]: Содержимое первой сноски.
[^2]: Содержимое второй сноски.
```

---

## 🔧 Команды MkDocs

### Разработка

```bash
# Запуск dev сервера
uv run mkdocs serve

# На другом порте
uv run mkdocs serve --dev-addr=localhost:8001

# С verbose выводом
uv run mkdocs serve --verbose
```

### Сборка

```bash
# Сборка статических файлов
uv run mkdocs build

# В другую директорию
uv run mkdocs build --site-dir=public

# Строгий режим (fail on warnings)
uv run mkdocs build --strict

# С очисткой старых файлов
uv run mkdocs build --clean
```

### Публикация

```bash
# Деплой на GitHub Pages (ручной)
uv run mkdocs gh-deploy

# С кастомным сообщением
uv run mkdocs gh-deploy -m "Updated docs"

# Без подтверждения
uv run mkdocs gh-deploy --force
```

---

## 🤖 Автоматическая публикация

### GitHub Actions Workflow

Документация автоматически публикуется при push в `main`:

```yaml
# .github/workflows/deploy-docs.yml
name: Deploy Documentation

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - 'mkdocs.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - run: uv sync --group docs
      - run: uv run mkdocs gh-deploy --force
```

### Процесс публикации

1. Внесите изменения в `docs/`
2. Закоммитьте: `git commit -m "docs: update guide"`
3. Запушьте: `git push origin main`
4. GitHub Actions автоматически:
   - Соберет документацию
   - Опубликует на GitHub Pages
   - Документация доступна через ~1-2 минуты

---

## 📊 Автогенерация отчетов

### Генерация отчетов об экспериментах

```bash
# Запуск скрипта генерации
python scripts/generate_experiment_report.py

# Результат: docs/reports/generated/latest_experiments.md
```

### Интеграция в Makefile

```makefile
.PHONY: generate-reports
generate-reports:
    @echo "Generating experiment reports..."
    uv run python scripts/generate_experiment_report.py
    @echo "✓ Reports generated"

.PHONY: docs-with-reports
docs-with-reports: generate-reports
    uv run mkdocs serve
```

Использование:

```bash
# Генерация отчетов + запуск docs сервера
make docs-with-reports
```

---

## 🎨 Кастомизация

### Стили

Отредактируйте `docs/stylesheets/extra.css`:

```css
/* Пример: изменение цвета заголовков */
h1, h2, h3 {
    color: #1976d2;
}

/* Пример: кастомный бейдж */
.badge-custom {
    background-color: #ff4081;
    color: white;
    padding: 0.25em 0.6em;
    border-radius: 0.375rem;
}
```

### JavaScript

Отредактируйте `docs/javascripts/extra.js` (создайте если нужно):

```javascript
// Пример: Google Analytics
window.dataLayer = window.dataLayer || [];
function gtag(){dataLayer.push(arguments);}
gtag('js', new Date());
gtag('config', 'GA_MEASUREMENT_ID');
```

Подключите в `mkdocs.yml`:

```yaml
extra_javascript:
  - javascripts/extra.js
```

---

## 🐛 Troubleshooting

### Проблема: Страница не обновляется

**Решение:**
```bash
# Перезапустите с очисткой
uv run mkdocs serve --clean

# Или очистите вручную
rm -rf site/
uv run mkdocs serve
```

### Проблема: Битые ссылки

**Решение:**
```bash
# Строгая сборка покажет ошибки
uv run mkdocs build --strict

# Исправьте найденные ссылки
# Используйте относительные пути: ../guides/index.md
```

### Проблема: Плагин не работает

**Решение:**
```bash
# Переустановите зависимости
uv sync --group docs --reinstall

# Проверьте конфигурацию
uv run mkdocs --version
uv pip list | grep mkdocs
```

---

## 📚 Best Practices

### 1. Структура документа

- ✅ Один H1 заголовок на страницу
- ✅ Логическая иерархия H2 → H3 → H4
- ✅ Короткие параграфы (3-5 предложений)
- ✅ Используйте списки для перечислений

### 2. Контент

- ✅ Начинайте с краткого описания
- ✅ Добавляйте примеры кода
- ✅ Используйте адмонишны для важной информации
- ✅ Ссылки на связанные разделы в конце

### 3. Код

- ✅ Всегда указывайте язык: \`\`\`python
- ✅ Добавляйте комментарии к сложному коду
- ✅ Проверяйте работоспособность примеров
- ✅ Используйте реальные примеры из проекта

### 4. Навигация

- ✅ Четкие названия разделов
- ✅ Индексные страницы с обзором
- ✅ Breadcrumbs (хлебные крошки)
- ✅ "Следующие шаги" в конце

### 5. Изображения

- ✅ Используйте описательные alt-тексты
- ✅ Оптимизируйте размер (< 500 KB)
- ✅ Храните в `docs/images/` или по разделам
- ✅ Используйте векторные форматы где возможно

---

## 🎓 Шаблоны

### Шаблон руководства

```markdown
# Название руководства

Краткое описание (1-2 предложения).

---

## 🎯 Что вы узнаете

- Пункт 1
- Пункт 2
- Пункт 3

---

## 📋 Предварительные требования

- Требование 1
- Требование 2

---

## 🚀 Шаг 1: Название

Описание шага.

\`\`\`bash
# Команды
\`\`\`

---

## 🔍 Проверка

Как проверить, что все работает.

---

## 📚 Следующие шаги

- [Связанная тема 1](link.md)
- [Связанная тема 2](link.md)

---

## 🆘 Troubleshooting

### Проблема X

**Решение:**
\`\`\`bash
command
\`\`\`
```

---

## 💡 Советы

1. **Live reload**: Не нужно перезапускать сервер при изменениях
2. **Search**: MkDocs автоматически индексирует весь контент
3. **Темная тема**: Переключатель в правом верхнем углу
4. **Mobile-friendly**: Документация адаптивна
5. **Проверяйте локально**: Всегда смотрите результат перед push

---

## 📖 Дополнительные ресурсы

- [MkDocs Documentation](https://www.mkdocs.org/)
- [Material for MkDocs](https://squidfunk.github.io/mkdocs-material/)
- [Python Markdown](https://python-markdown.github.io/)
- [PyMdown Extensions](https://facelessuser.github.io/pymdown-extensions/)
- [Markdown Guide](https://www.markdownguide.org/)

---

*Счастливого документирования! 📝✨*
