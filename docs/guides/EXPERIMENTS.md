# 🧪 Руководство по запуску экспериментов

Это руководство описывает процесс обучения моделей и отслеживание экспериментов с помощью DVCLive.

## 📋 Содержание

1. [Обзор](#обзор)
2. [Предварительные требования](#предварительные-требования)
3. [Запуск обучения](#запуск-обучения)
4. [Параметры модели](#параметры-модели)
5. [Отслеживание экспериментов](#отслеживание-экспериментов)
6. [Примеры запуска](#примеры-запуска)
7. [Анализ результатов](#анализ-результатов)
8. [Устранение неполадок](#устранение-неполадок)

---

## Обзор

Скрипт обучения `src/modeling/train.py` позволяет:
- Обучать модель **Random Forest** на данных Boston Housing
- Настраивать гиперпараметры через командную строку
- Автоматически логировать метрики через **DVCLive**
- Сохранять обученные модели для последующего использования

---

## Предварительные требования

### 1. Установка зависимостей

```bash
# Через uv (рекомендуется)
uv sync

# Или через pip
pip install -r requirements.txt
```

### 2. Наличие данных

Данные должны находиться в `data/raw/housing.csv`. Если их нет — загрузите через DVC:

```bash
# Запустите MinIO (если ещё не запущен)
docker-compose up -d minio

# Скачайте данные
dvc pull
```

### 3. Проверка установки

```bash
# Проверка доступности скрипта
python src/modeling/train.py --help
```

---

## Запуск обучения

### Базовый запуск (параметры по умолчанию)

```bash
python src/modeling/train.py
```

### Запуск с кастомными параметрами

```bash
python src/modeling/train.py \
    --n-estimators 200 \
    --max-depth 15 \
    --min-samples-split 10 \
    --test-size 0.25
```

### Короткий синтаксис

```bash
python src/modeling/train.py -n 200 -d 15 -s 10 -t 0.25
```

---

## Параметры модели

| Параметр | Короткая форма | По умолчанию | Описание |
|----------|----------------|--------------|----------|
| `--n-estimators` | `-n` | 100 | Количество деревьев в лесу |
| `--max-depth` | `-d` | 10 | Максимальная глубина деревьев (0 = без ограничений) |
| `--min-samples-split` | `-s` | 5 | Минимальное число образцов для разбиения узла |
| `--min-samples-leaf` | `-l` | 2 | Минимальное число образцов в листовом узле |
| `--test-size` | `-t` | 0.2 | Доля данных для тестовой выборки (0.0-1.0) |
| `--random-state` | `-r` | 42 | Seed для воспроизводимости результатов |
| `--data-path` | — | auto | Путь к файлу данных |

### Подробнее о параметрах

#### `--n-estimators` (количество деревьев)
Больше деревьев = лучше качество, но дольше обучение.
- **Рекомендация**: начните с 100, увеличивайте до 500 если позволяет время

#### `--max-depth` (глубина деревьев)
Контролирует сложность модели.
- `0` — без ограничений (деревья растут до полного разделения)
- `5-10` — для простых датасетов
- `15-20` — для сложных зависимостей

#### `--test-size` (размер тестовой выборки)
- `0.2` — стандартное соотношение 80/20
- `0.3` — больше данных для тестирования
- `0.1` — больше данных для обучения

---

## Отслеживание экспериментов

### DVCLive

Скрипт автоматически логирует через **DVCLive**:

**Параметры:**
- Все гиперпараметры модели
- Размер выборки (train/test)
- Количество признаков

**Метрики:**
- `r2_score` — коэффициент детерминации (чем ближе к 1, тем лучше)
- `rmse` — среднеквадратичная ошибка
- `mae` — средняя абсолютная ошибка
- `mape` — средняя абсолютная процентная ошибка

**Артефакты:**
- Обученная модель (`data/models/random_forest.pkl`)

### Просмотр результатов

После запуска эксперимента создаётся директория `dvclive/` с:
- `metrics.json` — финальные метрики
- `params.yaml` — параметры эксперимента
- `report.html` — визуальный отчёт

```bash
# Открыть отчёт в браузере
start dvclive/report.html  # Windows
open dvclive/report.html   # macOS
xdg-open dvclive/report.html  # Linux
```

---

## Примеры запуска

### Пример 1: Быстрый тест

Небольшая модель для быстрой проверки работоспособности:

```bash
python src/modeling/train.py -n 10 -d 5 -t 0.3
```

### Пример 2: Базовый эксперимент

Стандартные параметры для начального эксперимента:

```bash
python src/modeling/train.py
```

### Пример 3: Глубокая модель

Модель с большей ёмкостью:

```bash
python src/modeling/train.py \
    --n-estimators 300 \
    --max-depth 20 \
    --min-samples-split 2 \
    --min-samples-leaf 1
```

### Пример 4: Регуляризованная модель

Модель с защитой от переобучения:

```bash
python src/modeling/train.py \
    --n-estimators 200 \
    --max-depth 8 \
    --min-samples-split 10 \
    --min-samples-leaf 5
```

### Пример 5: Без ограничения глубины

```bash
python src/modeling/train.py -n 150 -d 0
```

### Пример 6: Использование альтернативного датасета

```bash
python src/modeling/train.py --data-path data/processed/housing_clean.csv
```

---

## Анализ результатов

### Интерпретация метрик

| Метрика | Отлично | Хорошо | Приемлемо | Плохо |
|---------|---------|--------|-----------|-------|
| R² Score | > 0.90 | 0.80-0.90 | 0.70-0.80 | < 0.70 |
| RMSE | < 2.5 | 2.5-3.5 | 3.5-4.5 | > 4.5 |
| MAE | < 2.0 | 2.0-2.5 | 2.5-3.5 | > 3.5 |
| MAPE | < 10% | 10-15% | 15-20% | > 20% |

### Важность признаков

После обучения в логах выводятся топ-5 важных признаков:

```
📊 Важность признаков:
  LSTAT: 0.4521
  RM: 0.3156
  DIS: 0.0687
  CRIM: 0.0412
  NOX: 0.0298
```

**Основные признаки Boston Housing:**
- `LSTAT` — % населения с низким статусом
- `RM` — среднее число комнат
- `DIS` — расстояние до центров занятости
- `CRIM` — уровень преступности
- `NOX` — концентрация оксидов азота

### Сравнение экспериментов

#### Через DVC

```bash
# Просмотр истории экспериментов
dvc exp show

# Сравнение двух экспериментов
dvc exp diff exp-xxxxx exp-yyyyy

# Визуализация через DVC Studio
dvc studio login
```

#### Вручную

Сохраняйте результаты в `data/experiments/`:

```bash
# Создать директорию для эксперимента
mkdir -p data/experiments/exp_001_baseline

# Скопировать результаты
cp -r dvclive/* data/experiments/exp_001_baseline/
cp data/models/random_forest.pkl data/experiments/exp_001_baseline/
```

---

## Устранение неполадок

### "Файл данных не найден"

**Симптом:**
```
ERROR: Файл данных не найден: data/raw/housing.csv
```

**Решение:**
```bash
# Убедитесь, что MinIO запущен
docker-compose up -d minio

# Загрузите данные
dvc pull

# Проверьте наличие файла
ls data/raw/
```

### Низкое качество модели

**Симптомы:** R² < 0.7, высокий RMSE

**Решения:**
1. Увеличьте количество деревьев: `-n 300`
2. Увеличьте глубину: `-d 15` или `-d 0`
3. Уменьшите регуляризацию: `-s 2 -l 1`

### Переобучение (overfitting)

**Симптомы:** R² на train > 0.95, но на test < 0.8

**Решения:**
1. Уменьшите глубину: `-d 8`
2. Увеличьте min_samples: `-s 10 -l 5`
3. Увеличьте test_size: `-t 0.3`

### Долгое обучение

**Решения:**
1. Уменьшите количество деревьев: `-n 50`
2. Ограничьте глубину: `-d 10`
3. Увеличьте min_samples_leaf: `-l 5`

### DVCLive не создаёт отчёт

**Решение:**
```bash
# Установите/переустановите dvclive
pip install dvclive --upgrade

# Проверьте наличие директории
ls dvclive/
```

---

## 🚀 Быстрый старт (TL;DR)

```bash
# 1. Убедитесь, что данные есть
dvc pull

# 2. Запустите базовый эксперимент
python src/modeling/train.py

# 3. Запустите с кастомными параметрами
python src/modeling/train.py -n 200 -d 15 -t 0.25

# 4. Посмотрите результаты
start dvclive/report.html

# 5. Сохраните удачный эксперимент
dvc add data/models/random_forest.pkl
git add data/models/random_forest.pkl.dvc dvclive/
git commit -m "exp: RF n=200 d=15, R²=0.89"
dvc push
```

---

## 📚 Полезные ссылки

- [Документация DVCLive](https://dvc.org/doc/dvclive)
- [Scikit-learn RandomForestRegressor](https://scikit-learn.org/stable/modules/generated/sklearn.ensemble.RandomForestRegressor.html)
- [Click Documentation](https://click.palletsprojects.com/)
- [Boston Housing Dataset](https://www.cs.toronto.edu/~delve/data/boston/bostonDetail.html)

