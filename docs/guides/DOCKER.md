# 🐳 Руководство по запуску экспериментов в Docker

Это руководство описывает запуск ML-экспериментов в изолированном Docker-контейнере для гарантированной воспроизводимости.

## 📋 Содержание

1. [Обзор](#обзор)
2. [Предварительные требования](#предварительные-требования)
3. [Архитектура контейнеров](#архитектура-контейнеров)
4. [Сборка образов](#сборка-образов)
5. [Запуск экспериментов](#запуск-экспериментов)
6. [Примеры использования](#примеры-использования)
7. [Работа с результатами](#работа-с-результатами)
8. [Устранение неполадок](#устранение-неполадок)

---

## Обзор

Проект включает два Docker-контейнера:

| Контейнер | Назначение | Порты |
|-----------|------------|-------|
| `minio` | S3-хранилище для данных и моделей | 9000, 9001 |
| `train` | ML-приложение для обучения моделей | — |

**Преимущества Docker:**
- 🔄 **Воспроизводимость** — одинаковое окружение везде
- 📦 **Изоляция** — нет конфликтов зависимостей
- 🚀 **Портативность** — запуск на любой машине с Docker
- 🔧 **Версионирование** — образ = snapshot окружения

---

## Предварительные требования

### Установка Docker

**Windows:**
1. Скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2. Установите и перезагрузите компьютер
3. Проверьте установку:
```bash
docker --version
docker-compose --version
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install docker.io docker-compose
sudo usermod -aG docker $USER
# Перелогиньтесь для применения прав
```

**macOS:**
```bash
brew install --cask docker
# Или скачайте Docker Desktop
```

---

## Архитектура контейнеров

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Network                        │
│                 boston_housing_network                   │
│                                                          │
│  ┌──────────────────┐      ┌──────────────────────────┐ │
│  │      MinIO       │      │         Train            │ │
│  │                  │      │                          │ │
│  │  S3 Storage      │      │  Python 3.13 + uv        │ │
│  │  :9000 (API)     │      │  scikit-learn            │ │
│  │  :9001 (Console) │      │  DVCLive                 │ │
│  │                  │      │                          │ │
│  │  ./minio_data    │      │  Volumes:                │ │
│  │       ↓          │      │  ./data/models           │ │
│  │    /data         │      │  ./dvclive               │ │
│  └──────────────────┘      └──────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### Файловая структура Docker

```
docker/
├── Dockerfile.app      # ML-приложение для обучения
└── Dockerfile.minio    # MinIO хранилище

docker-compose.yml      # Оркестрация контейнеров
```

---

## Сборка образов

### Сборка всех образов

```bash
# Из корня проекта
docker-compose build
```

### Сборка отдельных образов

```bash
# Только MinIO
docker-compose build minio

# Только ML-приложение
docker-compose build train
```

### Пересборка с нуля (без кэша)

```bash
docker-compose build --no-cache train
```

---

## Запуск экспериментов

### Базовый запуск

```bash
# Запуск с параметрами по умолчанию
docker-compose run --rm train
```

**Параметры по умолчанию:**
- `n_estimators`: 100
- `max_depth`: 10

### Запуск с кастомными параметрами

```bash
# Полный синтаксис
docker-compose run --rm train \
    --n-estimators 200 \
    --max-depth 15 \
    --min-samples-split 5 \
    --test-size 0.25

# Короткий синтаксис
docker-compose run --rm train -n 200 -d 15 -s 5 -t 0.25
```

### Доступные параметры

| Параметр | Короткая форма | По умолчанию | Описание |
|----------|----------------|--------------|----------|
| `--n-estimators` | `-n` | 100 | Количество деревьев |
| `--max-depth` | `-d` | 10 | Максимальная глубина (0 = без ограничений) |
| `--min-samples-split` | `-s` | 5 | Мин. образцов для разбиения |
| `--min-samples-leaf` | `-l` | 2 | Мин. образцов в листе |
| `--test-size` | `-t` | 0.2 | Доля тестовой выборки |
| `--random-state` | `-r` | 42 | Seed для воспроизводимости |

---

## Примеры использования

### Пример 1: Быстрый тест

Проверка работоспособности с минимальной моделью:

```bash
docker-compose run --rm train -n 10 -d 5
```

**Ожидаемый результат:** ~5-10 секунд, модель с низким качеством

### Пример 2: Базовый эксперимент

Стандартные параметры для начального эксперимента:

```bash
docker-compose run --rm train
```

**Ожидаемый результат:** R² ~ 0.85-0.88

### Пример 3: Глубокая модель

Модель с большей ёмкостью для лучшего качества:

```bash
docker-compose run --rm train -n 300 -d 20 -s 2 -l 1
```

**Ожидаемый результат:** R² ~ 0.88-0.91, время ~ 30-60 секунд

### Пример 4: Регуляризованная модель

Защита от переобучения:

```bash
docker-compose run --rm train -n 200 -d 8 -s 10 -l 5
```

**Ожидаемый результат:** R² ~ 0.82-0.86, устойчивая модель

### Пример 5: Серия экспериментов

Запуск нескольких экспериментов подряд:

```bash
# PowerShell
@(
    "-n 50 -d 5",
    "-n 100 -d 10",
    "-n 200 -d 15",
    "-n 300 -d 20"
) | ForEach-Object {
    Write-Host "Running: $_"
    docker-compose run --rm train $_.Split(" ")
}

# Bash
for params in "-n 50 -d 5" "-n 100 -d 10" "-n 200 -d 15" "-n 300 -d 20"; do
    echo "Running: $params"
    docker-compose run --rm train $params
done
```

---

## Работа с результатами

### Где сохраняются результаты

После каждого эксперимента результаты автоматически сохраняются на хост-машине:

```
project/
├── data/
│   └── models/
│       └── random_forest.pkl    # Обученная модель
├── dvclive/
│   ├── metrics.json             # Финальные метрики
│   ├── params.yaml              # Параметры эксперимента
│   └── plots/                   # Графики метрик
│       └── metrics/
│           ├── r2_score.tsv
│           ├── rmse.tsv
│           ├── mae.tsv
│           └── mape.tsv
└── data/
    └── experiments/             # Архив экспериментов
```

### Просмотр метрик

```bash
# PowerShell
Get-Content dvclive/metrics.json | ConvertFrom-Json

# Bash
cat dvclive/metrics.json | python -m json.tool
```

**Пример вывода:**
```json
{
    "r2_score": 0.8784,
    "rmse": 2.9862,
    "mae": 2.0685,
    "mape": 11.21
}
```

### Просмотр параметров

```bash
cat dvclive/params.yaml
```

**Пример вывода:**
```yaml
n_estimators: 200
max_depth: 15
min_samples_split: 5
min_samples_leaf: 2
random_state: 42
test_size: 0.25
```

### Сохранение эксперимента

```bash
# Создать директорию для эксперимента
mkdir -p data/experiments/exp_001_baseline

# Скопировать результаты
cp -r dvclive/* data/experiments/exp_001_baseline/
cp data/models/random_forest.pkl data/experiments/exp_001_baseline/

# Версионировать через DVC
dvc add data/models/random_forest.pkl
git add data/models/random_forest.pkl.dvc dvclive/
git commit -m "exp: RF n=200 d=15, R²=0.88"
```

---

## Полный рабочий процесс

### 1. Подготовка инфраструктуры

```bash
# Запуск MinIO (если нужно хранилище)
docker-compose up -d minio

# Проверка статуса
docker-compose ps
```

### 2. Сборка ML-образа

```bash
docker-compose build train
```

### 3. Запуск эксперимента

```bash
docker-compose run --rm train -n 200 -d 15
```

### 4. Анализ результатов

```bash
# Метрики
cat dvclive/metrics.json

# Параметры
cat dvclive/params.yaml
```

### 5. Сохранение результатов

```bash
dvc add data/models/random_forest.pkl
git add .
git commit -m "exp: RF experiment with n=200, d=15"
dvc push
git push
```

---

## Устранение неполадок

### "Cannot connect to Docker daemon"

**Причина:** Docker не запущен

**Решение:**
```bash
# Windows: запустите Docker Desktop
# Linux:
sudo systemctl start docker
```

### "Image not found"

**Причина:** Образ не собран

**Решение:**
```bash
docker-compose build train
```

### "Permission denied" при записи результатов

**Причина:** Проблемы с правами на volumes

**Решение (Linux):**
```bash
# Создать директории с правами
mkdir -p data/models dvclive data/experiments
chmod 777 data/models dvclive data/experiments
```

### Контейнер падает без ошибок

**Диагностика:**
```bash
# Запуск с интерактивным терминалом
docker-compose run --rm --entrypoint /bin/bash train

# Внутри контейнера
uv run python src/modeling/train.py --help
```

### Старые результаты в dvclive

**Причина:** DVCLive перезаписывает файлы

**Решение:** Сохраняйте результаты перед новым экспериментом:
```bash
# Архивировать предыдущий эксперимент
mv dvclive data/experiments/exp_$(date +%Y%m%d_%H%M%S)
```

### Долгая сборка образа

**Решение:** Используйте кэш Docker:
```bash
# Сборка с кэшем (по умолчанию)
docker-compose build train

# Очистка кэша если нужна чистая сборка
docker builder prune
docker-compose build --no-cache train
```

---

## 🚀 Быстрый старт (TL;DR)

```bash
# 1. Сборка образа
docker-compose build train

# 2. Запуск эксперимента
docker-compose run --rm train -n 200 -d 15

# 3. Просмотр результатов
cat dvclive/metrics.json

# 4. Сохранение
dvc add data/models/random_forest.pkl
git add . && git commit -m "exp: docker experiment"
```

---

## 📚 Полезные команды Docker

```bash
# Просмотр логов
docker-compose logs train

# Список образов
docker images | grep boston

# Удаление образов
docker-compose down --rmi all

# Очистка системы Docker
docker system prune -a

# Интерактивный режим
docker-compose run --rm --entrypoint /bin/bash train
```

---

## 📚 Полезные ссылки

- [Docker Documentation](https://docs.docker.com/)
- [Docker Compose Documentation](https://docs.docker.com/compose/)
- [Best Practices for Dockerfiles](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)
- [uv in Docker](https://docs.astral.sh/uv/guides/integration/docker/)

