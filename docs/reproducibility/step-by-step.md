# Пошаговая инструкция по воспроизведению проекта

Эта инструкция поможет вам полностью воспроизвести проект от начала до конца.

---

## 🎯 Цель

Полностью настроить рабочее окружение и запустить все компоненты проекта.

---

## ✅ Предварительные требования

### Минимальные

- ✅ Ubuntu 20.04+ / macOS 12+ / Windows 10+ с WSL2
- ✅ 4 GB RAM
- ✅ 2 GB свободного места на диске
- ✅ Доступ в интернет

### Для полной инфраструктуры

- ✅ 8 GB RAM (рекомендуется 16 GB)
- ✅ 10 GB свободного места на диске
- ✅ Docker + Docker Compose

---

## 📋 Шаг 1: Установка базовых инструментов

### 1.1. Python 3.13

=== "Ubuntu/Debian"

    ```bash
    sudo apt update
    sudo apt install software-properties-common
    sudo add-apt-repository ppa:deadsnakes/ppa
    sudo apt update
    sudo apt install python3.13 python3.13-venv python3.13-dev

    # Проверка
    python3.13 --version
    ```

=== "macOS"

    ```bash
    brew install python@3.13

    # Добавить в PATH
    echo 'export PATH="/opt/homebrew/opt/python@3.13/bin:$PATH"' >> ~/.zshrc
    source ~/.zshrc

    # Проверка
    python3.13 --version
    ```

=== "Windows (WSL2)"

    ```bash
    # В WSL2 терминале
    sudo apt update
    sudo apt install python3.13 python3.13-venv python3.13-dev

    # Проверка
    python3.13 --version
    ```

**✔️ Проверка:** `python3.13 --version` должна показать версию 3.13+

---

### 1.2. uv (пакетный менеджер)

```bash
# Установка
curl -LsSf https://astral.sh/uv/install.sh | sh

# Добавить в PATH (если нужно)
export PATH="$HOME/.cargo/bin:$PATH"
echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc

# Проверка
uv --version
```

**✔️ Проверка:** `uv --version` должна показать версию uv

---

### 1.3. Git

```bash
# Ubuntu/Debian
sudo apt install git

# macOS
brew install git

# Проверка
git --version
```

**✔️ Проверка:** `git --version` должна показать версию Git

---

### 1.4. Docker (опционально, для полной инфраструктуры)

=== "Ubuntu/Debian"

    ```bash
    # Установка Docker
    curl -fsSL https://get.docker.com -o get-docker.sh
    sudo sh get-docker.sh

    # Добавить пользователя в группу docker
    sudo usermod -aG docker $USER
    newgrp docker

    # Проверка
    docker --version
    docker-compose --version
    ```

=== "macOS"

    ```bash
    # Установка Docker Desktop
    brew install --cask docker

    # Запустите Docker Desktop из Applications

    # Проверка
    docker --version
    docker-compose --version
    ```

=== "Windows (WSL2)"

    1. Установите [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop)
    2. Включите WSL2 backend в настройках
    3. В WSL2 терминале проверьте:

    ```bash
    docker --version
    docker-compose --version
    ```

**✔️ Проверка:** Оба docker команды должны работать

---

## 📦 Шаг 2: Клонирование и настройка проекта

### 2.1. Клонирование репозитория

```bash
# Клонирование
git clone https://github.com/yourusername/ipml_boston_housing
cd ipml_boston_housing

# Проверка
ls -la
# Должны видеть: src/, conf/, airflow/, docker/, docs/, etc.
```

**✔️ Проверка:** Все папки на месте

---

### 2.2. Создание виртуального окружения

```bash
# Создание окружения
uv venv

# Активация
source .venv/bin/activate  # Linux/macOS
# или
.venv\Scripts\activate  # Windows

# Проверка
which python
# Должно показать путь к .venv/bin/python
```

**✔️ Проверка:** `which python` показывает .venv

---

### 2.3. Установка зависимостей

```bash
# Установка всех зависимостей
uv sync

# Установка dev зависимостей
uv sync --group dev

# Установка docs зависимостей
uv sync --group docs

# Проверка
uv pip list | wc -l
# Должно быть ~30+ пакетов
```

**✔️ Проверка:** Пакеты установлены без ошибок

---

## 🔧 Шаг 3: Настройка инструментов

### 3.1. Pre-commit хуки

```bash
# Установка хуков
uv run pre-commit install --install-hooks
uv run pre-commit install --hook-type commit-msg
uv run pre-commit install --hook-type pre-push

# Проверка
ls .git/hooks/pre-commit
# Файл должен существовать

# Тест
uv run pre-commit run --all-files
```

**✔️ Проверка:** Хуки установлены, тест прошел

---

### 3.2. Загрузка данных

**Вариант A: Через DVC (если настроен remote)**

```bash
dvc pull
```

**Вариант B: Прямая загрузка**

```bash
# Через Makefile
make download-data

# Или напрямую
python scripts/download_data.py
```

**✔️ Проверка:**

```bash
ls -lh data/raw/housing.csv
# Файл должен существовать (~30 KB)
```

---

## 🐳 Шаг 4: Запуск Docker инфраструктуры (опционально)

### 4.1. Запуск всех сервисов

```bash
# Запуск
docker-compose up -d

# Проверка статуса
docker-compose ps
# Все контейнеры должны быть "Up"
```

**✔️ Проверка:** Все контейнеры запущены

---

### 4.2. Проверка доступности сервисов

**Airflow UI:**
```bash
curl http://localhost:8080/health
# Должен вернуть {"status": "healthy"}

# Или откройте в браузере
open http://localhost:8080
# Логин: admin, Пароль: admin
```

**MLflow UI:**
```bash
curl -u admin:password http://localhost:5000/health
# Должен вернуть OK

# Или откройте в браузере
open http://localhost:5000
# Логин: admin, Пароль: password
```

**MinIO Console:**
```bash
open http://localhost:9001
# Логин: minioadmin, Пароль: minioadmin
```

**✔️ Проверка:** Все UI доступны

---

## 🚀 Шаг 5: Первый эксперимент

### 5.1. Базовое обучение

```bash
# Активируйте окружение (если не активировано)
source .venv/bin/activate

# Запуск обучения
uv run python src/modeling/train_hydra.py

# Должен вывести:
# - Загрузка данных...
# - Обучение модели...
# - Метрики: RMSE, R², MAE
```

**✔️ Проверка:**

```bash
# Проверьте метрики
cat dvclive/metrics.json

# Проверьте логи Hydra
ls -ltr outputs/
cat outputs/*/*/train_hydra.log

# Проверьте сохраненную модель
ls data/models/
```

---

### 5.2. Обучение с параметрами

```bash
# Смена модели
uv run python src/modeling/train_hydra.py model=gradient_boosting

# Переопределение параметров
uv run python src/modeling/train_hydra.py \
    model=random_forest \
    model.n_estimators=300 \
    model.max_depth=20
```

**✔️ Проверка:** Модели обучились с разными параметрами

---

### 5.3. Multirun (несколько моделей)

```bash
# Обучение нескольких моделей
uv run python src/modeling/train_hydra.py --multirun \
    model=ridge,lasso,elastic_net

# Должно обучить 3 модели последовательно
```

**✔️ Проверка:**

```bash
# Проверьте outputs/
ls outputs/*/*/.hydra/config.yaml
# Должно быть 3+ конфигурации
```

---

## 🔀 Шаг 6: Запуск Airflow DAG (опционально)

### 6.1. Открыть Airflow UI

```bash
open http://localhost:8080
# Логин: admin
# Пароль: admin
```

### 6.2. Запустить DAG

1. Найдите DAG: `boston_housing_simple`
2. Переключите toggle в положение "On"
3. Нажмите кнопку "Trigger DAG" (▶️)
4. Следите за выполнением в Graph View

**✔️ Проверка:** DAG успешно выполнился (все задачи зеленые)

---

### 6.3. Проверить результаты

```bash
# Результаты экспериментов
cat data/experiments/results_summary.csv

# Логи Airflow
docker-compose logs airflow-worker
```

---

## 📊 Шаг 7: Генерация отчетов

### 7.1. Генерация отчета об экспериментах

```bash
# Генерация отчета
python scripts/generate_experiment_report.py

# Или через Makefile
make generate-reports
```

**✔️ Проверка:**

```bash
# Отчет создан
ls docs/reports/generated/latest_experiments.md

# Графики созданы
ls docs/reports/plots/
```

---

### 7.2. Просмотр отчета

```bash
# В терминале
cat docs/reports/generated/latest_experiments.md

# Или запустите документацию
make docs-serve
# Откройте http://localhost:8000/reports/generated/latest_experiments/
```

---

## 📚 Шаг 8: Документация

### 8.1. Запуск локальной документации

```bash
# Установите зависимости (если не установлены)
uv sync --group docs

# Запуск сервера
make docs-serve
# или
uv run mkdocs serve

# Откройте в браузере
open http://localhost:8000
```

**✔️ Проверка:** Документация открывается в браузере

---

### 8.2. Изучение документации

Откройте следующие разделы:

1. **Быстрый старт** — базовая информация
2. **Руководства** — подробные гайды
3. **API Reference** — документация кода
4. **Примеры** — практические примеры
5. **Отчеты** — экспериментальные отчеты

---

## ✅ Шаг 9: Проверка окружения

### 9.1. Запуск скрипта проверки

```bash
python scripts/check_environment.py
```

**Ожидаемый вывод:**

```
=============================================================
           Boston Housing Environment Check
=============================================================

Checking Python Version...
✓ Python version: 3.13.x

Checking Virtual Environment...
✓ Virtual environment active: /path/to/.venv

Checking Project Structure...
✓ Directory present: src/
✓ Directory present: conf/
✓ Directory present: data/
...

Checking Dependencies...
✓ All dependencies installed: 28/28

Checking Data Files...
✓ Data file present: data/raw/housing.csv

=============================================================
                        Summary
=============================================================

Required checks: 7/7 passed
Optional checks: 4/4 passed

All required checks passed! ✓

You're ready to start!

Next steps:
  1. Run experiments: uv run python src/modeling/train_hydra.py
  2. Start Docker: make docker-up
  3. Open Airflow UI: http://localhost:8080
```

**✔️ Проверка:** Все проверки прошли успешно

---

## 🎉 Готово!

Поздравляем! Вы успешно настроили проект.

### Что дальше?

1. **Эксперименты** — запустите продвинутые эксперименты
2. **Airflow** — изучите другие DAG
3. **MLflow** — посмотрите результаты в UI
4. **Документация** — изучите все руководства

---

## 📋 Чеклист воспроизводимости

Используйте этот чеклист для проверки:

- [ ] Python 3.13+ установлен
- [ ] uv установлен и работает
- [ ] Git установлен
- [ ] Репозиторий склонирован
- [ ] Виртуальное окружение создано и активировано
- [ ] Все зависимости установлены (uv sync)
- [ ] Pre-commit хуки установлены
- [ ] Данные загружены (data/raw/housing.csv существует)
- [ ] Базовое обучение работает
- [ ] Скрипт проверки окружения прошел успешно

**Для полной инфраструктуры:**
- [ ] Docker установлен и работает
- [ ] Docker Compose установлен
- [ ] Все контейнеры запущены (docker-compose ps)
- [ ] Airflow UI доступен (http://localhost:8080)
- [ ] MLflow UI доступен (http://localhost:5000)
- [ ] MinIO Console доступен (http://localhost:9001)
- [ ] DAG успешно запустился и выполнился
- [ ] Отчеты сгенерированы
- [ ] Документация открывается локально

---

## 🆘 Troubleshooting

Если возникли проблемы, см. [Troubleshooting](troubleshooting.md)

Или запустите проверку:

```bash
python scripts/check_environment.py
```

---

## 📚 Дополнительные ресурсы

- [Воспроизводимость](index.md) — полная информация
- [Troubleshooting](troubleshooting.md) — решение проблем
- [Быстрый старт](../getting-started.md) — краткая инструкция

---

*Удачи в воспроизведении! 🚀*
