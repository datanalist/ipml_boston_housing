# Воспроизводимость проекта

Этот раздел содержит подробные инструкции для воспроизведения результатов проекта.

---

## 🎯 Цель

Обеспечить полную воспроизводимость всех аспектов проекта:

- ✅ Установка окружения
- ✅ Загрузка данных
- ✅ Запуск экспериментов
- ✅ Генерация отчетов
- ✅ Развертывание инфраструктуры

---

## 📋 Требования для полной воспроизводимости

### Минимальные требования

- **Python**: 3.13+
- **RAM**: 4 GB
- **Дисковое пространство**: 2 GB
- **ОС**: Linux, macOS, Windows (WSL2)

### Для полной инфраструктуры

- **Docker**: 24.0+
- **Docker Compose**: 2.20+
- **RAM**: 8 GB (рекомендуется 16 GB)
- **CPU**: 4+ ядер
- **Дисковое пространство**: 10 GB

---

## 🚀 Пошаговая инструкция

Детальные пошаговые инструкции с проверками на каждом этапе.

[→ Перейти к пошаговой инструкции](step-by-step.md)

---

## 🔍 Проверка зависимостей

Убедитесь, что все зависимости установлены корректно.

[→ Проверка зависимостей](dependencies.md)

---

## 🐛 Troubleshooting

Решение типичных проблем при воспроизведении.

[→ Troubleshooting](troubleshooting.md)

---

## 📊 Уровни воспроизводимости

### Уровень 1: Базовое обучение ⭐

**Время**: ~5 минут

**Что включает**:
- Установка Python окружения
- Обучение одной модели
- Базовые метрики

**Команды**:
```bash
make create_environment
make requirements
python src/modeling/train_hydra.py
```

---

### Уровень 2: Эксперименты с Hydra ⭐⭐

**Время**: ~15 минут

**Что включает**:
- Управление конфигурациями
- Обучение множества моделей
- MLflow трекинг

**Команды**:
```bash
make setup
python src/modeling/train_hydra.py --multirun model=ridge,lasso,rf
```

---

### Уровень 3: Полная инфраструктура ⭐⭐⭐

**Время**: ~30 минут

**Что включает**:
- Docker инфраструктура (Airflow + MLflow + MinIO)
- Оркестрация ML пайплайнов
- Версионирование данных с DVC
- Автоматическая генерация отчетов

**Команды**:
```bash
make setup
make docker-up
# Откройте http://localhost:8080 и запустите DAG
```

---

## 🔐 Фиксация версий

### Python зависимости

Проект использует `uv` с lock-файлом для полной воспроизводимости:

```bash
# uv.lock содержит точные версии всех зависимостей
uv sync  # Установит точно те же версии
```

### Docker образы

Все Docker образы фиксированы в `docker-compose.yml`:

```yaml
image: apache/airflow:2.10.4-python3.13
image: minio/minio:RELEASE-2025-01-01T00-00-00Z
```

### Данные и модели

DVC обеспечивает версионирование данных и моделей:

```bash
# Загрузка конкретной версии
dvc checkout

# Просмотр истории
dvc dag
```

---

## 🧪 Проверка воспроизводимости

### Автоматическая проверка

```bash
python scripts/check_environment.py
```

Скрипт проверит:
- ✅ Версию Python
- ✅ Установленные зависимости
- ✅ Наличие данных
- ✅ Конфигурационные файлы
- ✅ Docker (опционально)

### Ручная проверка

```bash
# 1. Python версия
python --version  # Должно быть 3.13+

# 2. Виртуальное окружение
which python  # Должно указывать на .venv

# 3. Зависимости
uv pip list | wc -l  # Должно быть ~30+ пакетов

# 4. Данные
ls data/raw/housing.csv  # Должен существовать

# 5. Docker (опционально)
docker-compose ps  # Должны быть запущены контейнеры
```

---

## 📝 Чеклист воспроизводимости

Используйте этот чеклист для проверки:

- [ ] Python 3.13+ установлен
- [ ] uv установлен
- [ ] Репозиторий склонирован
- [ ] Виртуальное окружение создано (`make create_environment`)
- [ ] Зависимости установлены (`make requirements`)
- [ ] Данные загружены (`make dvc-pull` или `make download-data`)
- [ ] Pre-commit хуки установлены (`make pre-commit`)
- [ ] Скрипт проверки прошел (`python scripts/check_environment.py`)
- [ ] Базовое обучение работает (`python src/modeling/train_hydra.py`)

**Для полной инфраструктуры:**
- [ ] Docker установлен
- [ ] Docker Compose установлен
- [ ] Контейнеры запущены (`make docker-up`)
- [ ] Airflow UI доступен (http://localhost:8080)
- [ ] MLflow UI доступен (http://localhost:5000)
- [ ] MinIO Console доступен (http://localhost:9001)
- [ ] DAG успешно запустился

---

## 🔄 Воспроизведение конкретного эксперимента

### Используя Hydra outputs

```bash
# Найти нужный эксперимент
ls outputs/

# Скопировать конфигурацию
cp outputs/2025-01-10/12-34-56/.hydra/config.yaml my_config.yaml

# Воспроизвести
python src/modeling/train_hydra.py --config-path=. --config-name=my_config
```

### Используя MLflow

```python
import mlflow

# Найти run
client = mlflow.tracking.MlflowClient()
run = client.search_runs(experiment_ids=["1"])[0]

# Получить параметры
params = run.data.params

# Воспроизвести с теми же параметрами
python src/modeling/train_hydra.py \
    model.n_estimators={params['n_estimators']} \
    model.max_depth={params['max_depth']}
```

---

## 🌍 Воспроизведение на разных ОС

### Linux

```bash
# Все работает из коробки
make setup
```

### macOS

```bash
# Убедитесь, что установлен Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установите зависимости
brew install python@3.13

# Продолжайте как обычно
make setup
```

### Windows (WSL2)

```bash
# Установите WSL2
wsl --install

# В WSL2 терминале
sudo apt update
sudo apt install python3.13 python3-pip

# Продолжайте как обычно
make setup
```

---

## 📚 Дополнительные ресурсы

- [Пошаговая инструкция](step-by-step.md) — детальное руководство
- [Проверка зависимостей](dependencies.md) — проверка установки
- [Troubleshooting](troubleshooting.md) — решение проблем
- [Быстрый старт](../getting-started.md) — минимальная установка

---

## ❓ Вопросы?

Если у вас возникли проблемы с воспроизводимостью:

1. Проверьте [Troubleshooting](troubleshooting.md)
2. Запустите `python scripts/check_environment.py`
3. Создайте [Issue на GitHub](https://github.com/yourusername/ipml_boston_housing/issues)

---

## 🎓 Философия воспроизводимости

Этот проект следует лучшим практикам воспроизводимости:

1. **Фиксация версий** — все зависимости зафиксированы
2. **Версионирование данных** — DVC для данных и моделей
3. **Документирование** — каждый шаг задокументирован
4. **Автоматизация** — Makefile и скрипты для всех операций
5. **Контейнеризация** — Docker для изоляции окружения
6. **Проверки** — автоматические проверки окружения
7. **Детальное логирование** — все эксперименты логируются

> "Воспроизводимость — это не опция, это необходимость для качественного ML проекта"
