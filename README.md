# 🏠 Boston Housing Price Prediction

<div align="center">

![Python](https://img.shields.io/badge/Python-3.13-blue?logo=python&logoColor=white)
![uv](https://img.shields.io/badge/uv-Package%20Manager-blueviolet)
![DVC](https://img.shields.io/badge/DVC-Data%20Version%20Control-945DD6?logo=dvc)
![MLflow](https://img.shields.io/badge/MLflow-Tracking-0194E2?logo=mlflow)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?logo=docker&logoColor=white)
![scikit-learn](https://img.shields.io/badge/scikit--learn-ML-F7931E?logo=scikitlearn&logoColor=white)

**Учебный ML-проект с лучшими инженерными практиками Data Science**

[📊 Отчёт о версионировании](#-отчёты) • [🚀 Быстрый старт](#-быстрый-старт) • [🐳 Docker](#-docker-инфраструктура) • [📚 Документация](#-документация)

</div>

---

## 📋 Описание

Проект прогнозирования медианной стоимости недвижимости в районах Бостона на основе социально-экономических и географических характеристик.

**Ключевые особенности:**
- 🔄 **Версионирование данных и моделей** — DVC + MinIO
- 📈 **Отслеживание экспериментов** — DVCLive + MLflow
- 🐳 **Воспроизводимость** — Docker + фиксация зависимостей (uv.lock)
- 🔒 **Качество кода** — pre-commit хуки (ruff, yamllint, shellcheck, detect-secrets)
- 📦 **14 моделей регрессии** — из коробки через model_loader

---

## 📄 Отчёты

| Отчёт | Описание |
|-------|----------|
| [**📊 Трекинг ML-экспериментов**](reports/LAB_REPORT.md) | MLflow, MinIO, декораторы, контекстные менеджеры |
| [📝 Настройка проекта](reports/PROJECT_SETUP_REPORT.md) | Структура, Git, pre-commit |

---

## ✅ Резюме проделанной работы

### 1. Настройка инструмента трекинга

| Компонент | Реализация |
|-----------|------------|
| **MLflow** | Установлен v2.18.0+ для трекинга экспериментов и Model Registry |
| **MinIO** | Развёрнуто S3-совместимое хранилище для артефактов |
| **Эксперименты** | Создан проект `boston-housing` с автоматическим логированием |
| **Аутентификация** | Двухуровневая защита: Nginx Basic Auth + MLflow Auth + MinIO Access Keys |

### 2. Проведение экспериментов

- **16 экспериментов** с различными алгоритмами:
  - Ансамбли: Random Forest, Gradient Boosting, AdaBoost, Bagging, Extra Trees
  - Линейные: Ridge, Lasso, ElasticNet, Huber Regressor
  - Другие: SVR, KNN, Decision Tree
- **Логирование**: параметры (`n_estimators`, `max_depth`), метрики (`r2_score`, `rmse`, `mae`), артефакты (модели, графики)
- **Сравнение**: через UI (таблицы, графики), API (`MlflowClient`), CLI (`mlflow runs`)
- **Фильтрация**: по метрикам, параметрам и тегам с комбинированными запросами

### 3. Интеграция с кодом

| Модуль | Назначение |
|--------|------------|
| `src/config/mlflow_config.py` | Конфигурация MLflow + MinIO |
| `src/tracking/decorators.py` | Декораторы `@mlflow_run`, `@log_params_decorator`, `@log_metrics_decorator` |
| `src/tracking/mlflow_tracker.py` | Контекстный менеджер `MLflowExperimentTracker` |
| `src/tracking/utils.py` | Утилиты: `get_best_run`, `load_best_model`, `compare_runs`, `register_best_model` |

### 4. Документация

- Полный отчёт [`reports/LAB_REPORT.md`](reports/LAB_REPORT.md) с описанием архитектуры
- Скриншоты: MinIO консоль, группировка экспериментов, метрики R², фильтрация
- Руководства в [`docs/guides/`](docs/guides/)

---

## 📊 Датасет

**Boston House Prices** — 506 объектов недвижимости, 13 признаков.

**Источник**: [Kaggle - Boston House Prices](https://www.kaggle.com/datasets/vikrishnan/boston-house-prices)

<details>
<summary><b>Описание признаков</b></summary>

| Признак | Описание |
|---------|----------|
| **CRIM** | Уровень преступности на душу населения |
| **ZN** | Доля жилых участков площадью >25,000 кв. футов |
| **INDUS** | Доля нежилого бизнеса |
| **CHAS** | Граничит ли участок с рекой Чарльз (0/1) |
| **NOX** | Концентрация оксидов азота |
| **RM** | Среднее количество комнат |
| **AGE** | Доля домов, построенных до 1940 года |
| **DIS** | Расстояние до центров занятости |
| **RAD** | Индекс доступности магистралей |
| **TAX** | Ставка налога на имущество |
| **PTRATIO** | Соотношение учеников/учителей |
| **B** | Показатель расового состава |
| **LSTAT** | % населения с низким статусом |
| **MEDV** | 🎯 Целевая: медианная стоимость ($1000) |

</details>

---

## 🛠️ Технологии

| Категория | Инструменты |
|-----------|-------------|
| **Язык** | Python 3.13 |
| **Пакетный менеджер** | [uv](https://docs.astral.sh/uv/) |
| **Версионирование данных** | DVC + MinIO (S3-совместимое хранилище) |
| **Эксперименты** | DVCLive, MLflow |
| **ML** | scikit-learn (14 моделей регрессии) |
| **Контейнеризация** | Docker, Docker Compose |
| **Качество кода** | pre-commit, ruff, yamllint, shellcheck |
| **Безопасность** | detect-secrets, Conventional Commits |

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.13
- [uv](https://docs.astral.sh/uv/getting-started/installation/)
- Docker + Docker Compose (для полной инфраструктуры)

### Установка

```bash
# 1. Клонирование репозитория
git clone <repo-url>
cd ipml_boston_housing

# 2. Полная автоматическая настройка (рекомендуется)
make setup
# Создаст окружение, установит зависимости, pre-commit хуки, скачает данные, запустит Docker

# --- ИЛИ пошаговая установка ---

# 2a. Создание окружения и установка зависимостей
make create_environment
make requirements

# 2b. Настройка pre-commit хуков
make pre-commit

# 2c. Загрузка данных
make dvc-pull

# 2d. Запуск инфраструктуры (опционально)
make docker-up
```

### Запуск обучения

```bash
# Локально
python src/modeling/train.py -n 200 -d 15

# Через Docker
docker-compose run --rm train -n 200 -d 15

# Просмотр результатов
cat dvclive/metrics.json
```

---

## 🐳 Docker-инфраструктура

```
┌──────────────────────────────────────────────────────────────┐
│                 boston_housing_network                        │
│                                                               │
│   ┌─────────────┐   ┌─────────────┐   ┌─────────────────┐   │
│   │    MinIO    │   │    Nginx    │   │     MLflow      │   │
│   │             │   │             │   │                 │   │
│   │  S3 :9000   │   │ Proxy :5000 │──▶│   Tracking      │   │
│   │  UI :9001   │   │ Basic Auth  │   │   sqlite + S3   │   │
│   └─────────────┘   └─────────────┘   └─────────────────┘   │
│          │                                     │             │
│          └─────────────────────────────────────┘             │
│                           │                                   │
│                  ┌────────▼────────┐                         │
│                  │      Train      │                         │
│                  │                 │                         │
│                  │  Python + uv    │                         │
│                  │  scikit-learn   │                         │
│                  │  DVCLive        │                         │
│                  └─────────────────┘                         │
└──────────────────────────────────────────────────────────────┘
```

### Сервисы

| Сервис | Порт | Описание |
|--------|------|----------|
| **MinIO** | 9000 (API), 9001 (Console) | S3-совместимое хранилище для данных и артефактов |
| **MLflow** | — (через Nginx) | Tracking Server для экспериментов |
| **Nginx** | 5000 | Reverse proxy с Basic Auth для MLflow |
| **Train** | — | Контейнер для обучения моделей |

### Команды

```bash
# Запуск инфраструктуры
docker-compose up -d minio

# Обучение модели
docker-compose run --rm train -n 200 -d 15

# Полный стек (MinIO + MLflow + Nginx)
docker-compose up -d

# Остановка
docker-compose down
```

---

## 📦 Доступные модели

Модуль `src/ml_models/model_loader.py` предоставляет 14 моделей регрессии:

| Категория | Модели |
|-----------|--------|
| **Линейные** | `linear_regression`, `ridge`, `lasso`, `elastic_net`, `huber`, `sgd` |
| **Ансамбли** | `random_forest`, `extra_trees`, `gradient_boosting`, `adaboost`, `bagging` |
| **Другие** | `decision_tree`, `svr`, `knn` |

```bash
# CLI: список моделей
python src/ml_models/model_loader.py list

# Создать модель
python src/ml_models/model_loader.py create random_forest

# Создать все модели
python src/ml_models/model_loader.py create --all

# Информация о модели
python src/ml_models/model_loader.py info gradient_boosting
```

```python
# Программное использование
from src.ml_models.model_loader import create_model, save_model, load_model

model = create_model("random_forest", custom_params={"n_estimators": 200})
save_model(model, "my_rf")
model = load_model("random_forest")
```

---

## 🔄 Версионирование (DVC)

```bash
# Загрузка данных
dvc pull

# Сохранение изменений
dvc add data/raw
dvc add data/models/random_forest.pkl
dvc push

# История экспериментов
dvc exp show

# Метрики
dvc metrics show
```

---

## 📁 Структура проекта

```
ipml_boston_housing/
│
├── config/                   # Конфигурации сервисов
│   ├── mlflow/              # MLflow: basic_auth.ini
│   └── nginx/               # Nginx: nginx.conf
│
├── data/                     # Данные (DVC-версионируемые)
│   ├── raw/                 # Исходные данные (housing.csv)
│   ├── models/              # Обученные модели (.pkl)
│   ├── raw.dvc              # DVC-метаданные
│   └── models.dvc
│
├── docker/                   # Docker-файлы
│   ├── Dockerfile.app       # ML-приложение
│   ├── Dockerfile.minio     # MinIO
│   ├── Dockerfile.mlflow    # MLflow
│   ├── entrypoint.sh        # Entrypoints
│   └── entrypoint-nginx.sh
│
├── docs/                     # Документация
│   ├── guides/              # Руководства
│   │   ├── DOCKER.md
│   │   ├── EXPERIMENTS.md
│   │   ├── MINIO+DVC.md
│   │   ├── MLFLOW+DVC+MINIO.md
│   │   └── PRE-COMMIT.md
│   └── mkdocs.yml
│
├── dvclive/                  # DVCLive: метрики и параметры
│   ├── metrics.json
│   ├── params.yaml
│   └── plots/
│
├── reports/                  # Отчёты
│   ├── LAB_REPORT.md        # Основной отчёт
│   ├── PROJECT_SETUP_REPORT.md
│   └── figures/
│
├── src/                      # Исходный код
│   ├── config.py            # Конфигурация
│   ├── dataset.py           # Работа с данными
│   ├── features.py          # Инженерия признаков
│   ├── plots.py             # Визуализация
│   ├── ml_models/           # Загрузчик моделей
│   │   └── model_loader.py  # 14 моделей scikit-learn
│   └── modeling/
│       ├── train.py         # Обучение
│       └── predict.py       # Предсказания
│
├── tests/                    # Тесты
│
├── .pre-commit-config.yaml   # Pre-commit хуки
├── docker-compose.yml        # Docker Compose
├── dvc.yaml                  # DVC pipeline
├── Makefile                  # Автоматизация
├── pyproject.toml            # Зависимости
└── uv.lock                   # Lock-файл (воспроизводимость)
```

---

## 📈 Метрики лучшей модели

| Метрика | Значение | Интерпретация |
|---------|----------|---------------|
| **R² Score** | 0.8665 | Хорошо (>0.80) |
| **RMSE** | 3.129 | Приемлемо |
| **MAE** | 2.090 | Хорошо (<2.5) |
| **MAPE** | 11.34% | Хорошо (10-15%) |

**Параметры модели (Random Forest):**
- `n_estimators`: 200
- `max_depth`: 15
- `min_samples_split`: 5
- `min_samples_leaf`: 2

---

## 🔧 Разработка

### Makefile-команды

```bash
# Быстрый старт
make help                # Все доступные команды
make setup               # Полная настройка: env + deps + pre-commit + dvc + docker

# Окружение и зависимости
make create_environment  # Создать виртуальное окружение
make requirements        # Установить зависимости (uv sync)
make pre-commit          # Установить все pre-commit хуки

# Качество кода
make lint                # Проверка кода (ruff)
make format              # Форматирование кода
make test                # Запуск тестов
make clean               # Очистка .pyc и __pycache__

# DVC (версионирование данных)
make dvc-pull            # Скачать данные из remote
make dvc-push            # Загрузить данные в remote
make dvc-status          # Проверить статус DVC

# Docker-инфраструктура
make docker-up           # Запустить MinIO, MLflow, Nginx
make docker-down         # Остановить контейнеры
make docker-logs         # Просмотр логов
make docker-build        # Сборка образов
make docker-status       # Статус контейнеров

# ML-эксперименты
make experiments         # Запуск экспериментов (требуется MLflow)
make train               # Обучение в Docker-контейнере
make run                 # Запуск main.py
make data                # Обработка датасета
```

### Pre-commit хуки

Проект использует комплексную систему проверок:

| Категория | Хуки |
|-----------|------|
| **Общие** | trailing-whitespace, end-of-file-fixer, check-yaml/json/toml |
| **Python** | ruff-check, ruff-format |
| **YAML** | yamllint |
| **Shell** | shellcheck |
| **Безопасность** | detect-secrets, detect-private-key |
| **Git** | commitizen (Conventional Commits) |

```bash
# Установка всех хуков (pre-commit, commit-msg, pre-push)
make pre-commit

# Или вручную
uv run pre-commit install --install-hooks
uv run pre-commit install --hook-type commit-msg
uv run pre-commit install --hook-type pre-push

# Ручной запуск проверок
uv run pre-commit run --all-files
```

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| [`docs/guides/MINIO+DVC.md`](docs/guides/MINIO+DVC.md) | Настройка MinIO и DVC |
| [`docs/guides/EXPERIMENTS.md`](docs/guides/EXPERIMENTS.md) | Запуск экспериментов |
| [`docs/guides/DOCKER.md`](docs/guides/DOCKER.md) | Работа с Docker |
| [`docs/guides/MLFLOW+DVC+MINIO.md`](docs/guides/MLFLOW+DVC+MINIO.md) | Интеграция MLflow |
| [`docs/guides/PRE-COMMIT.md`](docs/guides/PRE-COMMIT.md) | Настройка pre-commit |

---

## 📖 Полезные ресурсы

- [Cookiecutter Data Science](https://cookiecutter-data-science.drivendata.org/) — философия организации проекта
- [uv Documentation](https://docs.astral.sh/uv/) — документация пакетного менеджера
- [DVC Documentation](https://dvc.org/doc) — версионирование данных
- [MLflow Documentation](https://mlflow.org/docs/latest/index.html) — отслеживание экспериментов
- [scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html) — машинное обучение

---

## 👨‍🎓 Автор

Макаров Михаил Владимирович с ипользованием ИИ

## 📄 Лицензия

Проект создан в образовательных целях.
