# 🏠 Boston Housing Price Prediction

<div align="center">

![Python](https://img.shields.io/badge/Python-3.13-blue?logo=python&logoColor=white)
![uv](https://img.shields.io/badge/uv-Package%20Manager-blueviolet)
![Airflow](https://img.shields.io/badge/Airflow-2.10+-017CEE?logo=apacheairflow&logoColor=white)
![Hydra](https://img.shields.io/badge/Hydra-1.3+-89CFF0?logo=data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMTYiIGhlaWdodD0iMTYiIHZpZXdCb3g9IjAgMCAxNiAxNiIgZmlsbD0ibm9uZSIgeG1sbnM9Imh0dHA6Ly93d3cudzMub3JnLzIwMDAvc3ZnIj48cGF0aCBkPSJNOCAwQzMuNTgyIDAgMCAzLjU4MiAwIDhzMy41ODIgOCA4IDggOC0zLjU4MiA4LTgtMy41ODItOC04LTh6IiBmaWxsPSIjZmZmIi8+PC9zdmc+)
![DVC](https://img.shields.io/badge/DVC-Data%20Version%20Control-945DD6?logo=dvc)
![MLflow](https://img.shields.io/badge/MLflow-Tracking-0194E2?logo=mlflow)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?logo=docker&logoColor=white)
![scikit-learn](https://img.shields.io/badge/scikit--learn-ML-F7931E?logo=scikitlearn&logoColor=white)

**Учебный ML-проект с лучшими инженерными практиками Data Science**

[📖 Подробный отчёт](reports/LAB_REPORT.md) • [📊 Отчёт о версионировании](#-отчёты) • [🚀 Быстрый старт](#-быстрый-старт) • [🐳 Docker](#-docker-инфраструктура) • [📚 Документация](#-документация)

</div>

---

## 📋 Описание

Проект прогнозирования медианной стоимости недвижимости в районах Бостона на основе социально-экономических и географических характеристик.

**Ключевые особенности:**
- 🔀 **Оркестрация ML пайплайнов** — Apache Airflow 2.10+ (3 DAG, до 19 моделей параллельно)
- ⚙️ **Управление конфигурациями** — Hydra 1.3+ (14 моделей, Pydantic валидация, композиция)
- 🔄 **Версионирование данных и моделей** — DVC + MinIO
- 📈 **Отслеживание экспериментов** — DVCLive + MLflow
- 🐳 **Воспроизводимость** — Docker + фиксация зависимостей (uv.lock)
- 🔒 **Качество кода** — pre-commit хуки (ruff, yamllint, shellcheck, detect-secrets)
- 📦 **14 моделей регрессии** — из коробки через model_loader

## 📚 Документация

Полная документация проекта доступна на **GitHub Pages** и локально через MkDocs.

### 📖 Онлайн документация

**[→ Открыть документацию на GitHub Pages](https://datanalist.github.io/ipml_boston_housing/)**

### 🖥️ Локальная документация

```bash
# Установка зависимостей для документации
uv sync --group docs

# Запуск локального сервера с live reload
make docs-serve
# или
uv run mkdocs serve

# Откройте в браузере
open http://localhost:8000
```

### 📁 Структура документации

| Раздел | Описание | Ссылка |
|--------|----------|--------|
| **Быстрый старт** | Установка и первый запуск | [getting-started.md](docs/getting-started.md) |
| **Руководства** | Подробные гайды по всем компонентам | [guides/](docs/guides/index.md) |
| **Развертывание** | Локальное и Docker развертывание | [deployment/](docs/deployment/index.md) |
| **API Reference** | Документация кода | [api/](docs/api/index.md) |
| **Примеры** | Практические примеры использования | [examples/](docs/examples/index.md) |
| **Отчеты** | Экспериментальные отчеты | [reports/](docs/reports/index.md) |
| **Воспроизводимость** | Инструкции по воспроизведению | [reproducibility/](docs/reproducibility/index.md) |

### 🔨 Работа с документацией

```bash
# Создание/редактирование документации
vim docs/guides/my-guide.md

# Локальный просмотр с live reload
make docs-serve

# Генерация отчетов об экспериментах
make generate-reports

# Проверка окружения
make check-env

# Сборка статических файлов
make docs-build

# Деплой на GitHub Pages (автоматически через CI/CD)
git add docs/
git commit -m "docs: update documentation"
git push  # GitHub Actions автоматически опубликует
```

**Возможности документации:**
- ✅ Автоматическая генерация API-документации (mkdocstrings)
- ✅ Поддержка формул (MathJax)
- ✅ Dark/Light режимы
- ✅ Полнотекстовый поиск
- ✅ Автоматическая публикация на GitHub Pages
- ✅ Preview для Pull Requests

См. [Работа с документацией](docs/guides/DOCUMENTATION.md) для подробностей.

---

## 📊 Отчеты

### Автоматическая генерация отчетов

Проект включает полноценную систему автоматической генерации отчетов об экспериментах с визуализациями:

```bash
# Генерация отчета
python scripts/generate_experiment_report.py

# Или через Makefile
make generate-reports

# Проверка окружения перед запуском
python scripts/check_environment.py
# или
make check-env
```

**Отчеты включают:**
- 📈 **Визуализации метрик** (4 типа графиков)
  - Сравнение RMSE/R²/MAE/времени обучения
  - Scatter plot RMSE vs R² с категоризацией моделей
- 📋 **Сравнительные таблицы** всех моделей с автоматическим выделением лучших значений
- 🏆 **Анализ лучших моделей** с детальными метриками
- 💡 **Рекомендации и выводы** на основе результатов
- 📊 **Статистика** по всем экспериментам

**Результаты:**
- Markdown отчет: `docs/reports/generated/latest_experiments.md`
- Графики (PNG): `docs/reports/plots/`
- Автоматически интегрируются в документацию MkDocs
- Поддержка разных форматов данных экспериментов

**Технологии:**
- **Jinja2** — шаблонизация отчетов
- **matplotlib** — создание графиков
- **seaborn** — стилизация визуализаций
- **pandas** — анализ данных экспериментов

См. [Отчеты об экспериментах](docs/reports/index.md) для подробностей.

---

## 📄 Основные отчёты проекта

| Отчёт | Описание |
|-------|----------|
| [**📊 Трекинг ML-экспериментов**](reports/LAB_REPORT.md) | MLflow, MinIO, декораторы, контекстные менеджеры |
| [📝 Настройка проекта](reports/PROJECT_SETUP_REPORT.md) | Структура, Git, pre-commit |
| [📈 Экспериментальные отчеты](docs/reports/generated/) | Автогенерируемые отчеты об экспериментах с графиками |
| [📚 Документация проекта](https://datanalist.github.io/ipml_boston_housing/) | Полная онлайн-документация на GitHub Pages |

---

## ✅ Реализованные возможности

### Документация и отчетность

Проект оснащен профессиональной системой документации и автоматической генерации отчетов, обеспечивающей полную прозрачность и воспроизводимость исследований.

**Техническая документация (MkDocs + Material):**
- 📖 **7 основных разделов** — от быстрого старта до API Reference
- 🎨 **Material theme** с поддержкой dark/light режимов
- 🔍 **Полнотекстовый поиск** по всей документации
- 📝 **30+ страниц** детальной документации
- 🔄 **Автогенерация API** через mkdocstrings
- 🧮 **Поддержка формул** через MathJax
- 📱 **Адаптивный дизайн** для всех устройств

**GitHub Pages:**
- ✅ **Автоматическая публикация** через GitHub Actions
- ✅ **CI/CD pipeline** для документации
- ✅ **Preview для PR** — предпросмотр изменений
- ✅ **Автообновление** при push в main/master

**Система генерации отчетов:**
- 📊 **Автоматические отчеты** об экспериментах в Markdown
- 📈 **4 типа визуализаций** (metrics comparison, scatter plots)
- 📋 **Сравнительные таблицы** с выделением лучших результатов
- 💡 **Аналитика и рекомендации** на основе метрик
- 🔄 **Интеграция с MLflow** и DVCLive

**Воспроизводимость:**
- ✅ **Скрипт проверки окружения** (`check_environment.py`)
- ✅ **Пошаговые инструкции** по развертыванию
- ✅ **Troubleshooting** с решениями типичных проблем
- ✅ **Автоматические проверки** всех компонентов

### Оркестрация ML-пайплайнов

Проект использует **Apache Airflow 2.10+** для управления и автоматизации машинного обучения. Развёрнута полная инфраструктура на базе Docker Compose с Celery Executor, PostgreSQL для метаданных и Redis в качестве брокера очередей.

**Реализованы три типа workflow:**
- **boston_housing_simple** — базовый линейный пайплайн для быстрого прототипирования
- **boston_housing_experiments** — параллельное обучение 19 моделей с автоматической агрегацией результатов
- **boston_housing_cached** — интеллектуальный пайплайн с кэшированием в MinIO и условным выполнением задач

Настроены зависимости между этапами: последовательная загрузка и валидация данных, параллельное обучение моделей в трёх группах (Linear/Tree/Other), агрегация результатов и генерация отчётов. Система поддерживает до 16 параллельных задач и автоматически пропускает обучение при наличии закэшированных моделей с актуальным хэшем данных.

### Управление конфигурациями

Для гибкого управления экспериментами интегрирован **Hydra 1.3+** с полной поддержкой композиции и валидации. Созданы конфигурации для 14 алгоритмов машинного обучения, организованные в иерархическую структуру с поддержкой наследования и переопределения параметров.

Все конфигурации валидируются через **Pydantic schemas**, обеспечивая типобезопасность и проверку диапазонов значений на этапе загрузки. Реализована система композиции с готовыми пресетами экспериментов (`baseline`, `tuned`, `linear_comparison`), позволяющая быстро запускать серии экспериментов с различными комбинациями параметров через `--multirun`.

### Интеграция и мониторинг

Все инструменты интегрированы в единую экосистему:
- **MLflow** для трекинга экспериментов с автоматическим логированием параметров, метрик и артефактов
- **MinIO** как S3-совместимое хранилище для версионирования данных и моделей
- **DVC** для воспроизводимости экспериментов
- **DVCLive** для real-time мониторинга метрик

Настроена система мониторинга через Airflow Web UI с визуализацией графа выполнения, логами задач и метриками производительности. Реализовано автоматическое сохранение конфигураций Hydra и результатов экспериментов, обеспечивающее полную воспроизводимость. Проект протестирован на совместимость всех компонентов и корректность работы пайплайнов с различными конфигурациями.

---

## 📊 Датасет

**Boston House Prices** — 506 объектов недвижимости, 13 признаков.

**Источник**: [Kaggle - Boston House Prices](https://www.kaggle.com/datasets/vikrishnan/boston-house-prices)

<details>
<summary><b>Описание признаков</b></summary>

| Признак | Описание |
|---------|----------|
| **CRIM** | Уровень преступности на душу населения |
| **ZN** | Доля жилых участков площадью >25,000 кв. футов |
| **INDUS** | Доля нежилого бизнеса |
| **CHAS** | Граничит ли участок с рекой Чарльз (0/1) |
| **NOX** | Концентрация оксидов азота |
| **RM** | Среднее количество комнат |
| **AGE** | Доля домов, построенных до 1940 года |
| **DIS** | Расстояние до центров занятости |
| **RAD** | Индекс доступности магистралей |
| **TAX** | Ставка налога на имущество |
| **PTRATIO** | Соотношение учеников/учителей |
| **B** | Показатель расового состава |
| **LSTAT** | % населения с низким статусом |
| **MEDV** | 🎯 Целевая: медианная стоимость ($1000) |

</details>

---

## 🛠️ Технологии

| Категория | Инструменты |
|-----------|-------------|
| **Язык** | Python 3.13 |
| **Пакетный менеджер** | [uv](https://docs.astral.sh/uv/) |
| **Оркестрация** | Apache Airflow 2.10+ (Celery Executor, PostgreSQL, Redis) |
| **Конфигурации** | Hydra 1.3+ (иерархические конфигурации, Pydantic валидация) |
| **Версионирование данных** | DVC + MinIO (S3-совместимое хранилище) |
| **Эксперименты** | DVCLive, MLflow |
| **ML** | scikit-learn (14 моделей регрессии) |
| **Контейнеризация** | Docker, Docker Compose |
| **Качество кода** | pre-commit, ruff, yamllint, shellcheck |
| **Безопасность** | detect-secrets, Conventional Commits |

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.13
- [uv](https://docs.astral.sh/uv/getting-started/installation/)
- Docker + Docker Compose (для полной инфраструктуры)

### Установка

```bash
# 1. Клонирование репозитория
git clone <repo-url>
cd ipml_boston_housing

# 2. Полная автоматическая настройка (рекомендуется)
make setup
# Создаст окружение, установит зависимости, pre-commit хуки, скачает данные, запустит Docker

# --- ИЛИ пошаговая установка ---

# 2a. Создание окружения и установка зависимостей
make create_environment
make requirements

# 2b. Настройка pre-commit хуков
make pre-commit

# 2c. Загрузка данных
make dvc-pull

# 2d. Запуск инфраструктуры (опционально)
make docker-up
```

### Запуск обучения

**Вариант 1: Hydra (рекомендуется) — гибкое управление конфигурациями**

```bash
# Базовый запуск (Random Forest по умолчанию)
uv run python src/modeling/train_hydra.py

# Смена модели
uv run python src/modeling/train_hydra.py model=gradient_boosting
uv run python src/modeling/train_hydra.py model=ridge
uv run python src/modeling/train_hydra.py model=lasso

# Переопределение параметров
uv run python src/modeling/train_hydra.py model=random_forest model.n_estimators=500 model.max_depth=20

# Готовые эксперименты
uv run python src/modeling/train_hydra.py +experiment=tuned
uv run python src/modeling/train_hydra.py +experiment=baseline
uv run python src/modeling/train_hydra.py +experiment=linear_comparison

# Multirun — параллельное обучение нескольких моделей
uv run python src/modeling/train_hydra.py --multirun model=ridge,lasso,elastic_net
uv run python src/modeling/train_hydra.py --multirun model=random_forest,gradient_boosting,extra_trees
```

**Вариант 2: Airflow DAG — оркестрация через Web UI**

1. Запустите Airflow: `docker-compose up -d`
2. Откройте http://localhost:8080 (admin/admin)
3. Выберите и запустите DAG:
   - `boston_housing_simple` — одна модель
   - `boston_housing_experiments` — 19 моделей параллельно
   - `boston_housing_cached` — с кэшированием

**Вариант 3: Классический CLI**

```bash
# Локально
python src/modeling/train.py -n 200 -d 15

# Через Docker
docker-compose run --rm train -n 200 -d 15
```

**Просмотр результатов:**

```bash
# Метрики DVCLive
cat dvclive/metrics.json

# Конфигурация Hydra
cat outputs/2025-12-23/12-06-27/.hydra/config.yaml

# Логи Hydra
cat outputs/2025-12-23/12-06-27/train_hydra.log

# MLflow UI
open http://localhost:5000

# Airflow UI
open http://localhost:8080
```

---

## 🐳 Docker-инфраструктура

```
┌────────────────────────────────────────────────────────────────────────────┐
│                        boston_housing_network                               │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                     AIRFLOW ORCHESTRATION                             │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │  │
│  │  │  Webserver   │  │  Scheduler   │  │    Worker    │               │  │
│  │  │   :8080      │  │              │  │   (Celery)   │               │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘               │  │
│  │         │                 │                  │                        │  │
│  │         └─────────────────┼──────────────────┘                        │  │
│  │                           │                                           │  │
│  │         ┌─────────────────┴──────────────────┐                        │  │
│  │         │                                     │                        │  │
│  │  ┌──────▼───────┐                    ┌───────▼──────┐                │  │
│  │  │  PostgreSQL  │                    │    Redis     │                │  │
│  │  │  (Metadata)  │                    │   (Broker)   │                │  │
│  │  └──────────────┘                    └──────────────┘                │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                      ML TRACKING & STORAGE                            │  │
│  │  ┌─────────────┐   ┌─────────────┐   ┌─────────────────┐            │  │
│  │  │    MinIO    │   │    Nginx    │   │     MLflow      │            │  │
│  │  │             │   │             │   │                 │            │  │
│  │  │  S3 :9000   │   │ Proxy :5000 │──▶│   Tracking      │            │  │
│  │  │  UI :9001   │   │ Basic Auth  │   │   sqlite + S3   │            │  │
│  │  └─────────────┘   └─────────────┘   └─────────────────┘            │  │
│  │         │                                      │                      │  │
│  │         └──────────────────────────────────────┘                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│                               ┌────────────────┐                            │
│                               │  Train (CLI)   │                            │
│                               │                │                            │
│                               │  Python + uv   │                            │
│                               │  Hydra Config  │                            │
│                               │  scikit-learn  │                            │
│                               │  DVCLive       │                            │
│                               └────────────────┘                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### Сервисы

| Сервис | Порт | Описание |
|--------|------|----------|
| **Airflow Webserver** | 8080 | Web UI для управления DAG (логин: admin/admin) |
| **Airflow Scheduler** | — | Планировщик задач и мониторинг DAG |
| **Airflow Worker** | — | Исполнитель задач через Celery (4 параллельных воркера) |
| **PostgreSQL** | 5432 | База метаданных Airflow |
| **Redis** | 6379 | Брокер очередей для Celery |
| **MinIO** | 9000 (API), 9001 (Console) | S3-совместимое хранилище для данных и артефактов |
| **MLflow** | — (через Nginx) | Tracking Server для экспериментов |
| **Nginx** | 5000 | Reverse proxy с Basic Auth для MLflow |
| **Train** | — | Контейнер для обучения моделей (CLI) |

### Команды

```bash
# Запуск полной инфраструктуры (Airflow + MLflow + MinIO)
docker-compose up -d

# Запуск только ML tracking (без Airflow)
docker-compose up -d minio mlflow nginx

# Проверка статуса сервисов
docker-compose ps

# Логи Airflow
docker-compose logs -f airflow-webserver
docker-compose logs -f airflow-scheduler
docker-compose logs -f airflow-worker

# Обучение модели через Hydra (локально)
uv run python src/modeling/train_hydra.py model=random_forest
uv run python src/modeling/train_hydra.py +experiment=tuned

# Обучение модели через CLI (Docker)
docker-compose run --rm train -n 200 -d 15

# Остановка инфраструктуры
docker-compose down

# Полная очистка (включая volumes)
docker-compose down -v
```

### Airflow Web UI

После запуска `docker-compose up -d` откройте:
- **URL**: http://localhost:8080
- **Логин**: admin
- **Пароль**: admin

**Доступные DAG:**
- `boston_housing_simple` — базовый линейный пайплайн
- `boston_housing_experiments` — параллельное обучение 19 моделей
- `boston_housing_cached` — пайплайн с кэшированием в MinIO

---

## 📦 Доступные модели

Модуль `src/ml_models/model_loader.py` предоставляет 14 моделей регрессии:

| Категория | Модели |
|-----------|--------|
| **Линейные** | `linear_regression`, `ridge`, `lasso`, `elastic_net`, `huber`, `sgd` |
| **Ансамбли** | `random_forest`, `extra_trees`, `gradient_boosting`, `adaboost`, `bagging` |
| **Другие** | `decision_tree`, `svr`, `knn` |

```bash
# CLI: список моделей
python src/ml_models/model_loader.py list

# Создать модель
python src/ml_models/model_loader.py create random_forest

# Создать все модели
python src/ml_models/model_loader.py create --all

# Информация о модели
python src/ml_models/model_loader.py info gradient_boosting
```

```python
# Программное использование
from src.ml_models.model_loader import create_model, save_model, load_model

model = create_model("random_forest", custom_params={"n_estimators": 200})
save_model(model, "my_rf")
model = load_model("random_forest")
```

---

## ⚙️ Управление конфигурациями (Hydra)

Проект использует **Hydra** для гибкого управления конфигурациями экспериментов.

### Структура конфигураций

```
conf/
├── config.yaml              # Главная конфигурация с defaults
├── model/                   # 14 конфигураций моделей
│   ├── random_forest.yaml
│   ├── gradient_boosting.yaml
│   ├── ridge.yaml
│   ├── lasso.yaml
│   ├── elastic_net.yaml
│   ├── linear_regression.yaml
│   ├── huber.yaml
│   ├── sgd.yaml
│   ├── decision_tree.yaml
│   ├── extra_trees.yaml
│   ├── adaboost.yaml
│   ├── bagging.yaml
│   ├── svr.yaml
│   └── knn.yaml
├── data/                    # Конфигурации данных
│   └── boston.yaml
├── training/                # Параметры обучения
│   └── default.yaml
└── experiment/              # Готовые эксперименты
    ├── baseline.yaml
    ├── tuned.yaml
    └── linear_comparison.yaml
```

### Использование через CLI

```bash
# Базовый запуск (Random Forest по умолчанию)
uv run python src/modeling/train_hydra.py

# Смена модели
uv run python src/modeling/train_hydra.py model=gradient_boosting

# Переопределение параметров
uv run python src/modeling/train_hydra.py model=random_forest \
    model.n_estimators=500 \
    model.max_depth=20 \
    training.test_size=0.3

# Готовый эксперимент
uv run python src/modeling/train_hydra.py +experiment=tuned

# Multirun (несколько моделей параллельно)
uv run python src/modeling/train_hydra.py --multirun model=ridge,lasso,elastic_net

# Комбинация параметров (grid search)
uv run python src/modeling/train_hydra.py --multirun \
    model=ridge \
    model.alpha=0.1,1.0,10.0
```

### Валидация конфигураций

Все конфигурации валидируются через **Pydantic schemas**:

| Schema | Назначение |
|--------|------------|
| `BaseConfig` | Базовый класс с общими настройками |
| `ModelConfig` | Базовая валидация параметров моделей |
| `RandomForestConfig` | Валидация Random Forest (n_estimators: 10-1000, max_depth: 1-50) |
| `GradientBoostingConfig` | Валидация Gradient Boosting (learning_rate: 0-1) |
| `RidgeConfig` | Валидация Ridge (alpha > 0) |
| `LassoConfig` | Валидация Lasso (alpha > 0) |
| `DataConfig` | Валидация параметров данных |
| `TrainingConfig` | Валидация параметров обучения |
| `ExperimentConfig` | Композиция всех конфигураций |

**Пример валидации:**

```python
from src.schemas import RandomForestConfig

# ✅ Валидная конфигурация
config = RandomForestConfig(
    name="random_forest",
    n_estimators=200,
    max_depth=15,
    random_state=42
)

# ❌ Ошибка валидации
config = RandomForestConfig(
    name="random_forest",
    n_estimators=5000  # ValueError: n_estimators должен быть <= 1000
)
```

### Преимущества Hydra

✅ **Иерархическая композиция** — наследование и переопределение конфигураций  
✅ **Типизация и валидация** — через Pydantic schemas  
✅ **Multirun** — параллельный запуск экспериментов  
✅ **Логирование** — автоматическое сохранение конфигураций в `outputs/`  
✅ **Переопределение** — через CLI без изменения файлов  
✅ **Воспроизводимость** — все конфигурации сохраняются

---

## 🔀 Оркестрация ML пайплайнов (Apache Airflow)

Проект использует **Apache Airflow** для управления и мониторинга ML пайплайнов.

### Архитектура Airflow

```
┌──────────────────────────────────────────────────────────┐
│                    Airflow Components                     │
│                                                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  Webserver   │  │  Scheduler   │  │    Worker    │   │
│  │   (Web UI)   │  │  (DAG Exec)  │  │   (Celery)   │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│         │                  │                  │           │
│         └──────────────────┼──────────────────┘           │
│                            │                              │
│         ┌──────────────────┴──────────────────┐           │
│         │                                     │           │
│  ┌──────▼───────┐                    ┌───────▼──────┐    │
│  │  PostgreSQL  │                    │    Redis     │    │
│  │  (Metadata)  │                    │   (Broker)   │    │
│  └──────────────┘                    └──────────────┘    │
└──────────────────────────────────────────────────────────┘
```

### Доступные DAG

| DAG | Описание | Задачи | Время выполнения |
|-----|----------|--------|------------------|
| **boston_housing_simple** | Базовый линейный пайплайн | 4 задачи последовательно | ~30 сек |
| **boston_housing_experiments** | Параллельное обучение 19 моделей | 25+ задач (параллельно) | ~2-3 мин |
| **boston_housing_cached** | Пайплайн с кэшированием в MinIO | 6 задач (условные) | ~10 сек (при кэше) |

### DAG: boston_housing_simple

**Архитектура:**

```
download_data → validate_data → train_model → evaluate_model → save_results
```

**Особенности:**
- Простой линейный пайплайн (proof of concept)
- Одна модель (Random Forest)
- Последовательное выполнение

### DAG: boston_housing_experiments

**Архитектура:**

```
                                    ┌→ linear_models (7 моделей)
download_data → validate_data ──────┼→ tree_models (9 моделей)
                                    └→ other_models (3 модели)
                                            │
                                            ▼
                                    aggregate_results → generate_report
```

**Особенности:**
- **19 экспериментов** с различными алгоритмами
- **Параллельное обучение** в 3 группах:
  - **Linear Models** (7): LinearRegression, Ridge (α=0.1,1,10), Lasso, ElasticNet, Huber
  - **Tree Models** (9): DecisionTree (d=5,10), RandomForest (n=100,200), ExtraTrees, GradientBoosting (2 варианта), AdaBoost, Bagging
  - **Other Models** (3): SVR (2 варианта), KNN
- Автоматическая агрегация результатов
- Экспорт в CSV: `data/experiments/results_summary.csv`

### DAG: boston_housing_cached

**Архитектура:**

```
download_data → check_cache ──┬→ (cache found)  → use_cached_model ──┐
                              │                                       │
                              └→ (no cache) → train_model            │
                                              ↓                       │
                                          save_to_cache               │
                                              ↓                       │
                                              └───────────────────────┤
                                                                      ▼
                                                              generate_summary
```

**Особенности:**
- **Интеллектуальное кэширование** в MinIO
- **ShortCircuitOperator** — пропускает обучение если модель в кэше
- **Проверка хэша данных** — обучение только при изменении данных
- **Условное выполнение** — `trigger_rule="none_failed"`
- **Экономия времени** — ~70% при использовании кэша

### Запуск Airflow

```bash
# 1. Запуск инфраструктуры
docker-compose up -d

# 2. Проверка статуса
docker-compose ps

# 3. Открыть Web UI
open http://localhost:8080

# Логин: admin
# Пароль: admin

# 4. Запуск DAG через UI
# Перейти в UI → DAGs → Выбрать DAG → Trigger DAG

# 5. Просмотр логов
docker-compose logs -f airflow-worker
docker-compose logs -f airflow-scheduler
```

### Мониторинг и отладка

```bash
# Статус всех сервисов
docker-compose ps

# Логи конкретного сервиса
docker-compose logs -f airflow-webserver
docker-compose logs -f airflow-scheduler
docker-compose logs -f airflow-worker
docker-compose logs -f postgres
docker-compose logs -f redis

# Перезапуск сервиса
docker-compose restart airflow-scheduler
docker-compose restart airflow-worker

# Выполнение команд внутри контейнера
docker-compose exec airflow-webserver airflow dags list
docker-compose exec airflow-webserver airflow tasks list boston_housing_experiments
```

### Параметры параллелизма

| Параметр | Значение | Описание |
|----------|----------|----------|
| `AIRFLOW__CORE__PARALLELISM` | 16 | Максимум параллельных задач в системе |
| `AIRFLOW__CORE__DAG_CONCURRENCY` | 8 | Максимум параллельных задач в одном DAG |
| `AIRFLOW__CELERY__WORKER_CONCURRENCY` | 4 | Количество воркеров Celery |

### Преимущества Airflow

✅ **Визуализация** — граф выполнения задач  
✅ **Мониторинг** — логи, метрики, статусы  
✅ **Параллелизм** — до 16 задач одновременно  
✅ **Кэширование** — пропуск выполненных задач  
✅ **Retry** — автоматический повтор при ошибках  
✅ **Масштабируемость** — Celery Executor с несколькими воркерами

---

## 🔄 Версионирование (DVC)

```bash
# Загрузка данных
dvc pull

# Сохранение изменений
dvc add data/raw
dvc add data/models/random_forest.pkl
dvc push

# История экспериментов
dvc exp show

# Метрики
dvc metrics show
```

---

## 📁 Структура проекта

```
ipml_boston_housing/
│
├── airflow/                  # Apache Airflow оркестрация
│   ├── dags/                # DAG определения
│   │   ├── boston_housing_simple.py       # Базовый пайплайн
│   │   ├── boston_housing_experiments.py  # 19 моделей параллельно
│   │   └── boston_housing_cached.py       # Пайплайн с кэшированием
│   ├── logs/                # Логи выполнения
│   └── plugins/             # Кастомные плагины (minio_cache)
│
├── conf/                     # Hydra конфигурации
│   ├── config.yaml          # Главная конфигурация
│   ├── model/               # 14 конфигураций моделей
│   │   ├── random_forest.yaml
│   │   ├── gradient_boosting.yaml
│   │   ├── ridge.yaml
│   │   └── ...
│   ├── data/                # Конфигурации данных
│   │   └── boston.yaml
│   ├── training/            # Параметры обучения
│   │   └── default.yaml
│   └── experiment/          # Готовые эксперименты
│       ├── baseline.yaml
│       ├── tuned.yaml
│       └── linear_comparison.yaml
│
├── config/                   # Конфигурации сервисов
│   ├── mlflow/              # MLflow: basic_auth.ini
│   └── nginx/               # Nginx: nginx.conf
│
├── data/                     # Данные (DVC-версионируемые)
│   ├── raw/                 # Исходные данные (housing.csv)
│   ├── models/              # Обученные модели (.pkl)
│   ├── experiments/         # Результаты экспериментов
│   ├── raw.dvc              # DVC-метаданные
│   └── models.dvc
│
├── docker/                   # Docker-файлы
│   ├── Dockerfile.app       # ML-приложение
│   ├── Dockerfile.airflow   # Airflow (scheduler/worker/webserver)
│   ├── Dockerfile.minio     # MinIO
│   ├── Dockerfile.mlflow    # MLflow
│   ├── entrypoint.sh        # Entrypoints
│   └── entrypoint-nginx.sh
│
├── docs/                     # Документация
│   ├── guides/              # Руководства
│   │   ├── DOCKER.md
│   │   ├── EXPERIMENTS.md
│   │   ├── MINIO+DVC.md
│   │   ├── MLFLOW+DVC+MINIO.md
│   │   └── PRE-COMMIT.md
│   └── mkdocs.yml
│
├── dvclive/                  # DVCLive: метрики и параметры
│   ├── metrics.json
│   ├── params.yaml
│   └── plots/
│
├── outputs/                  # Hydra outputs (логи, конфигурации)
│   └── YYYY-MM-DD/
│       └── HH-MM-SS/
│           ├── .hydra/      # Сохранённые конфигурации
│           └── train_hydra.log
│
├── reports/                  # Отчёты
│   ├── LAB_REPORT.md        # Основной отчёт
│   ├── PROJECT_SETUP_REPORT.md
│   └── figures/
│
├── src/                      # Исходный код
│   ├── config/              # Конфигурации
│   │   ├── __init__.py
│   │   └── mlflow_config.py
│   ├── schemas/             # Pydantic схемы для валидации
│   │   ├── base.py          # BaseConfig
│   │   ├── model_config.py  # 14 моделей с валидацией
│   │   ├── data_config.py   # Валидация данных
│   │   ├── training_config.py  # Валидация обучения
│   │   └── __init__.py
│   ├── tracking/            # MLflow трекинг
│   │   ├── decorators.py    # @mlflow_run, @log_params, @log_metrics
│   │   ├── mlflow_tracker.py  # MLflowExperimentTracker
│   │   ├── utils.py         # get_best_run, load_best_model
│   │   └── __init__.py
│   ├── ml_models/           # Загрузчик моделей
│   │   └── model_loader.py  # 14 моделей scikit-learn
│   ├── modeling/
│   │   ├── train.py         # Классическое обучение
│   │   ├── train_hydra.py   # Обучение с Hydra
│   │   └── predict.py       # Предсказания
│   ├── config.py            # Общая конфигурация
│   ├── dataset.py           # Работа с данными
│   ├── features.py          # Инженерия признаков
│   └── plots.py             # Визуализация
│
├── tests/                    # Тесты
│
├── .pre-commit-config.yaml   # Pre-commit хуки
├── docker-compose.yml        # Docker Compose (Airflow + MLflow + MinIO)
├── dvc.yaml                  # DVC pipeline
├── Makefile                  # Автоматизация
├── pyproject.toml            # Зависимости (uv)
└── uv.lock                   # Lock-файл (воспроизводимость)
```

---

## 📈 Метрики лучшей модели

| Метрика | Значение | Интерпретация |
|---------|----------|---------------|
| **R² Score** | 0.8665 | Хорошо (>0.80) |
| **RMSE** | 3.129 | Приемлемо |
| **MAE** | 2.090 | Хорошо (<2.5) |
| **MAPE** | 11.34% | Хорошо (10-15%) |

**Параметры модели (Random Forest):**
- `n_estimators`: 200
- `max_depth`: 15
- `min_samples_split`: 5
- `min_samples_leaf`: 2

---

## 🔧 Разработка

### Makefile-команды

```bash
# Быстрый старт
make help                # Все доступные команды
make setup               # Полная настройка: env + deps + pre-commit + dvc + docker
make quickstart          # Быстрый запуск: данные + инфраструктура + эксперименты

# Окружение и зависимости
make create_environment  # Создать виртуальное окружение
make requirements        # Установить зависимости (uv sync)
make pre-commit          # Установить все pre-commit хуки

# Качество кода
make lint                # Проверка кода (ruff)
make format              # Форматирование кода
make test                # Запуск тестов
make clean               # Очистка .pyc и __pycache__

# DVC (версионирование данных)
make dvc-pull            # Скачать данные из remote
make dvc-push            # Загрузить данные в remote
make dvc-status          # Проверить статус DVC

# Docker-инфраструктура (Airflow + MLflow + MinIO)
make docker-up           # Запустить всю инфраструктуру
make docker-down         # Остановить контейнеры
make docker-logs         # Просмотр логов
make docker-build        # Сборка образов
make docker-status       # Статус контейнеров
make docker-restart      # Перезапуск инфраструктуры
make docker-recreate     # Пересоздание Docker network (при ошибках)

# ML-эксперименты
make experiments         # Запуск экспериментов (требуется MLflow)
make train               # Обучение в Docker-контейнере
make run                 # Запуск main.py
make data                # Обработка датасета

# Документация и отчеты
make docs-serve          # Запуск локального сервера документации
make docs-build          # Сборка статических файлов документации
make generate-reports    # Генерация отчетов об экспериментах
make check-env           # Проверка окружения и зависимостей
make docs-with-reports   # Генерация отчетов + запуск docs-serve

# Загрузка данных
make download-data       # Скачать датасет из интернета
make download-data-force # Принудительная перезагрузка
```

### Pre-commit хуки

Проект использует комплексную систему проверок:

| Категория | Хуки |
|-----------|------|
| **Общие** | trailing-whitespace, end-of-file-fixer, check-yaml/json/toml |
| **Python** | ruff-check, ruff-format |
| **YAML** | yamllint |
| **Shell** | shellcheck |
| **Безопасность** | detect-secrets, detect-private-key |
| **Git** | commitizen (Conventional Commits) |

```bash
# Установка всех хуков (pre-commit, commit-msg, pre-push)
make pre-commit

# Или вручную
uv run pre-commit install --install-hooks
uv run pre-commit install --hook-type commit-msg
uv run pre-commit install --hook-type pre-push

# Ручной запуск проверок
uv run pre-commit run --all-files
```

---

## 📚 Дополнительная документация

| Файл | Описание |
|------|----------|
| [`docs/guides/DOCUMENTATION.md`](docs/guides/DOCUMENTATION.md) | Работа с документацией проекта |
| [`docs/guides/MINIO+DVC.md`](docs/guides/MINIO+DVC.md) | Настройка MinIO и DVC |
| [`docs/guides/EXPERIMENTS.md`](docs/guides/EXPERIMENTS.md) | Запуск экспериментов |
| [`docs/guides/DOCKER.md`](docs/guides/DOCKER.md) | Работа с Docker |
| [`docs/guides/MLFLOW+DVC+MINIO.md`](docs/guides/MLFLOW+DVC+MINIO.md) | Интеграция MLflow |
| [`docs/guides/PRE-COMMIT.md`](docs/guides/PRE-COMMIT.md) | Настройка pre-commit |
| [`docs/reproducibility/step-by-step.md`](docs/reproducibility/step-by-step.md) | Пошаговое воспроизведение |
| [**Полная документация на GitHub Pages →**](https://datanalist.github.io/ipml_boston_housing/) | **Все руководства, API, примеры** |

---

## 📖 Полезные ресурсы

- [Cookiecutter Data Science](https://cookiecutter-data-science.drivendata.org/) — философия организации проекта
- [uv Documentation](https://docs.astral.sh/uv/) — документация пакетного менеджера
- [DVC Documentation](https://dvc.org/doc) — версионирование данных
- [MLflow Documentation](https://mlflow.org/docs/latest/index.html) — отслеживание экспериментов
- [scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html) — машинное обучение

---

## 👨‍🎓 Автор

Макаров Михаил Владимирович с ипользованием ИИ

## 📄 Лицензия

Проект создан в образовательных целях.
